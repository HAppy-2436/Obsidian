# 中国剩余定理 (CRT)
# 陈述：若模数 mᵢ 两两互素，则系统 x ≡ aᵢ (mod mᵢ) 有唯一解，模 M = ∏ mᵢ。
# 要点：
# - 构造：令 M=∏ mᵢ，Mᵢ = M / mᵢ，则可取 x = Σ aᵢ · Mᵢ · inv(Mᵢ mod mᵢ)。
# - 唯一性：任意两解相差 M 的倍数。
# - 扩展：若模不互素，需一致性条件 aᵢ ≡ aⱼ (mod gcd(mᵢ,mⱼ)) 才有解。