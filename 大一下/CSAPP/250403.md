1. 0x44FA9000
2. 向正无穷大舍入：选择大于或等于原值的最小可表示数。
3. 向偶数舍入：非中间值舍入到最近值；中间值时使结果最低有效位为0。
4. ```c
   long decode2(long x, long y, long z) {
       z -= y;
       long t = z * x;
       return t ^ (t >> 63);
   }
   ```
5. ```c
   long loop(long x, int n) {
       long result = 0;
       long mask;
       for (mask = 1; mask != 0; mask <<= n)
           result |= mask & x;
       return result;
   }
   ```