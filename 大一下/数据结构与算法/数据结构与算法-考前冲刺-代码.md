---
title: 数据结构与算法 · 考前冲刺（按课程目录 + Lab 全覆盖，理论全掌握版）
tags: [MOC, 冲刺, 王丽萍]
source: 课程目录 12 章 + 25 个 Lab 51 道题
---

# 数据结构与算法 · 考前冲刺（理论全掌握版）

> 📚 **严格按 `00-课程目录.md` 12 章组织**；所有 51 道 Lab 题的 C++ 解题模板全部内嵌。
> 🧠 **理论部分**讲"是什么 / 怎么算 / 时间空间复杂度 / 适用场景 / 易错点 / 常见考点"，覆盖考试可能考到的**所有概念**。
> 💻 **代码部分**讲"输入输出格式 + 关键思路 + 完整 C++ 模板"，51 道 Lab 全部覆盖。

---

# 数据结构与算法 · 考前冲刺（代码实战版）

> 本文档侧重**Lab 代码实战**，每章保留精简骨架（关键概念速查表），然后是**完整 51 道 Lab 代码**。
> 理论详细讲解见《考前冲刺-知识点.md》。

---


- **理论速记 → 全部掌握**：每章开头先讲清楚所有概念、性质、复杂度、应用、坑位。Lab 涉及的章还会再展开对应专题。
- **Lab 必写代码**：每章内嵌，编号直接对应 `数据结构与算法-全部Lab题汇总.md` 里的 `questionId` / Lab 号。
- **代码语言**：C++（按课程教学语言）。
- **建议复习顺序**：第 2→3→4 章（线性结构）→ 5（递归）→ 6（列表）→ 7→8（查找排序）→ 9（哈希）→ 10（树）→ 1（绪论 Life Game 单独看）→ 11→12（多叉树/图）。

---

## 0.1 课程目录 ↔ Lab 覆盖度速查

| 课程章 | 知识点 | Lab 涉及 | 冲刺策略 |
|---|---|---|---|
| 第 1 章 课程介绍 | Life Game、C++ 类、数组 | ✅ Lab01, Lab21 | 理论 + 必写代码 |
| 第 2 章 栈 | 顺序栈、链栈、应用 | ✅ Lab02, Lab20, Lab23 | 理论 + 必写代码 |
| 第 3 章 队列 | 顺序队列（环形）、链队列、双端队列 | ✅ Lab03, Lab24 | 理论 + 必写代码 |
| 第 4 章 链式栈/队列 | 链栈、链队列、循环链表 | ✅ Lab04 约瑟夫, Lab05, Lab06 | 理论 + 必写代码 |
| 第 5 章 递归 | 递归分析、八皇后、井字 | ✅ Lab07, Lab25 | 理论 + 必写代码 |
| 第 6 章 列表 | 顺序表、单/双链表、改进 | ✅ Lab08, Lab09, Lab10 | 理论 + 必写代码 |
| 第 7 章 查找 | 顺序、二分 | ✅ Lab11, Lab12 | 理论 + 必写代码 |
| 第 8 章 排序 | 六大排序 + 堆 | ✅ Lab13, Lab14, Lab15 | 理论 + 必写代码 |
| 第 9 章 哈希 | 哈希函数、冲突处理 | ✅ Lab16, Lab17 | 理论 + 必写代码 |
| 第 10 章 二叉树 | 遍历、BST、AVL、Splay | ✅ Lab18, Lab19 | 理论 + 必写代码 |
| 第 11 章 多叉树 | Trie、B 树 | ❌ 无 Lab | **理论全掌握**（考试常考概念） |
| 第 12 章 图 | 存储、遍历、拓扑、最短路、MST | ❌ 无 Lab | **理论全掌握**（考试常考大题） |

---

## 0.2 Lab 知识点分布（13 个，覆盖 51 题）

| 知识点 | 题数 | 满分 |
|---|---:|---:|
| 排序 | 9 | 300 |
| 单链表 | 6 | 200 |
| 栈 | 5 | 300 |
| 队列 | 4 | 200 |
| 递归 | 4 | 200 |
| 查找 | 4 | 200 |
| 二叉树 | 4 | 200 |
| 数组与游戏 | 3 | 200 |
| 链式结构（栈/队列） | 3 | 200 |
| 线性表（链式） | 3 | 100 |
| 哈希表 | 3 | 200 |
| 线性表（顺序） | 2 | 100 |
| 线性表应用（字符串） | 1 | 100 |
| **合计** | **51** | **2500** |

---

## 0.3 难度分布与高分策略

- **38 题中等**（核心拿分区）：每题都需要看清输入输出格式、边界（`-1` 结束、`-1 -1` 结束）、空格 / 换行。
- **11 题困难**：二分查找的左右边界、链地址法哈希表的链表操作、堆排序的 buildHeap / siftDown、BST 删除 + 前驱、迷宫 BFS 最短路。
- **2 题简单**：中序波兰式、二进制转十进制（送分）。

---


## 第 1 章 课程介绍

### 章节速查

**核心主题**：复杂度分析 + ADT + Life Game 通用思路

| 概念 | 关键点 |
|---|---|
| 时间复杂度 | O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(2ⁿ) |
| 主定理 | T(n)=aT(n/b)+O(n^d)，比较 d 与 log_b(a) |
| ADT | 数据 + 操作 = 抽象数据类型 |
| Life Game 通用思路 | 当前态 + 下一态双数组，每代 O(n) |

**Lab 注意事项**：
- Life Game：行宽 60、1-indexed、邻居数 = 自身 + 左右各 1、2（共 4 个）
- Magic Square：行 = `(row-1+n)%n`、列 = `(col+1)%n`、撞占位正下方

### 本章 Lab 代码（完整）

### 1.2 Lab01-题目1：One-Dimensional Life（q136, 中等, 50 分）

**题意**：一维格子，每个活细胞有 4 个邻居（左右各 1、2）。规则：
- 死细胞：邻居数 = 2 或 3 → 复活
- 活细胞：邻居数 = 0/1/3 → 死
- 活细胞：邻居数 = 2 或 4 → 活
- 总数 < 60。

**输入**：活细胞位置（1..60），以 `-1` 结束；n 代。
**输出**：第 n 代行（`-` 空 / `*` 活）。

**关键点**：
- 行宽 60，位置 1..60（不是 0..59）
- n=0 输出初始行
- 每代用新数组存，邻居 = 自身 + 左右各 1、2（共 4 个）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    const int N = 60;
    vector<int> cur(N+5, 0), nxt(N+5, 0);
    int p;
    while (cin >> p && p != -1) cur[p] = 1;   // 1-indexed
    int g; cin >> g;
    for (int step = 1; step <= g; step++) {
        for (int i = 1; i <= N; i++) {
            int nb = cur[i-2] + cur[i-1] + cur[i+1] + cur[i+2];
            if (cur[i] == 1)
                nxt[i] = (nb == 2 || nb == 4) ? 1 : 0;
            else
                nxt[i] = (nb == 2 || nb == 3) ? 1 : 0;
        }
        cur = nxt;
    }
    for (int i = 1; i <= N; i++) cout << (cur[i] ? '*' : '-');
    cout << '\n';
    return 0;
}
```

### 1.3 Lab01-题目2：Magic Square（q135, 中等, 50 分）

**题意**：奇数 n（1..101）生成 n×n 魔方阵，方法：1 放顶行中间；后续数字沿右上对角走；撞顶/撞右则环绕；撞到已占位则正下方一格。

**关键点**：
- 行 = `(row - 1 + n) % n`，列 = `(col + 1) % n`
- 撞占位 → 行 = `(row + 1) % n`，列不变
- 输出：数字之间**两个空格**，行末无空格

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    vector<vector<int>> a(n, vector<int>(n, 0));
    int r = 0, c = n/2;        // 1 放 (0, n/2)
    a[r][c] = 1;
    for (int k = 2; k <= n*n; k++) {
        int nr = (r - 1 + n) % n;
        int nc = (c + 1) % n;
        if (a[nr][nc] != 0) {  // 撞占位
            nr = (r + 1) % n;
            nc = c;
        }
        a[nr][nc] = k;
        r = nr; c = nc;
    }
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if (j) cout << "  ";
            cout << a[i][j];
        }
        cout << '\n';
    }
    return 0;
}
```

### 1.4 Lab21：二维 Life Game（q134, 中等, 100 分）

**题意**：20×60 棋盘；8 邻居；规则 B3/S23。
- 活细胞：邻居 0/1 → 死；2/3 → 活；≥4 → 死
- 死细胞：邻居 = 3 → 活

**关键点**：
- 输出范围 20×60 全网格
- 死 → `-`，活 → `*`
- 每代双 buffer 切换

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    const int R = 20, C = 60;
    vector<vector<int>> cur(R, vector<int>(C, 0));
    int x, y;
    while (cin >> x >> y) {
        if (x == -1 && y == -1) break;
        cur[x][y] = 1;   // 题目说 (x, y) 行列都从 1 开始
    }
    int g; cin >> g;
    for (int step = 0; step < g; step++) {
        vector<vector<int>> nxt(R, vector<int>(C, 0));
        for (int i = 0; i < R; i++) {
            for (int j = 0; j < C; j++) {
                int nb = 0;
                for (int di = -1; di <= 1; di++)
                    for (int dj = -1; dj <= 1; dj++) {
                        if (di == 0 && dj == 0) continue;
                        int ni = i+di, nj = j+dj;
                        if (ni>=0 && ni<R && nj>=0 && nj<C)
                            nb += cur[ni][nj];
                    }
                if (cur[i][j]) nxt[i][j] = (nb==2 || nb==3);
                else nxt[i][j] = (nb==3);
            }
        }
        cur = nxt;
    }
    for (int i = 0; i < R; i++) {
        for (int j = 0; j < C; j++)
            cout << (cur[i][j] ? '*' : '-');
        cout << '\n';
    }
    return 0;
}
```

---


## 第 2 章 栈

### 章节速查

**核心主题**：LIFO 线性表 + 栈的应用

| 概念 | 关键点 |
|---|---|
| 栈顶约定 | `top=-1`（指向栈顶）/ `top=0`（指向空位）两种 |
| 顺序栈 | 进栈 `data[++top]=x`，出栈 `return data[top--]` |
| 链栈 | 头插法（头节点即栈顶），无栈满问题 |
| 应用 | 表达式求值（前后中序波兰式）、括号匹配、出入栈序列判断 |

**复杂度**：所有操作 O(1)

**Lab 注意事项**：
- 后序波兰式（逆波兰）：遇操作数入栈，遇运算符弹两数计算，结果入栈
- 出入栈判断：**模拟整个过程**——用栈模拟入栈，看出栈序列是否合法
- 括号匹配：左括号入栈，右括号检查栈顶是否匹配
- 中序波兰式（中缀→后缀）：用**运算符栈** + **操作数栈**，按优先级处理

### 本章 Lab 代码（完整）

### 2.2 Lab23：栈的顺序实现（q137, 困难, 100 分）

**题意**：实现 Stack（顺序存储），支持 pop/push/top/size/clear/full；用其将输入整数序列**逆序输出**。

**关键点**：
- 容量设为 100（题目说 <100）
- `top` 初始 -1
- 输入 `-1` 结束

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class Stack {
    static const int MAXN = 105;
    int data[MAXN];
    int top;
public:
    Stack(): top(-1) {}
    bool empty() const { return top == -1; }
    bool full()  const { return top == MAXN-1; }
    int  size()  const { return top + 1; }
    void clear() { top = -1; }
    bool push(int x) { if (full()) return false; data[++top] = x; return true; }
    bool pop()        { if (empty()) return false; top--; return true; }
    int  peek() const { return data[top]; }
};

int main() {
    Stack s;
    int x; 
    while (cin >> x && x != -1) s.push(x);
    bool first = true;
    while (!s.empty()) {
        if (!first) cout << ' ';
        first = false;
        cout << s.peek();
        s.pop();
    }
    cout << '\n';
    return 0;
}
```

### 2.3 Lab05-链式存储的栈（q158, 困难, 100 分）

**题意**：实现 `MLinkedStack`（链栈），支持 pop/push/top/size/clear/full；逆序输出。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

struct Node { int val; Node* next; Node(int v): val(v), next(nullptr) {} };

class MLinkedStack {
    Node* topNode;
    int   cnt;
public:
    MLinkedStack(): topNode(nullptr), cnt(0) {}
    ~MLinkedStack() { clear(); }
    bool empty() const { return cnt == 0; }
    bool full()  const { return false; }    // 链式永不满
    int  size()  const { return cnt; }
    void clear() { while (topNode) { Node* t = topNode; topNode = topNode->next; delete t; } cnt = 0; }
    void push(int x) { Node* n = new Node(x); n->next = topNode; topNode = n; cnt++; }
    void pop()        { Node* t = topNode; topNode = topNode->next; delete t; cnt--; }
    int  top() const  { return topNode->val; }
};

int main() {
    MLinkedStack s;
    int x; while (cin >> x && x != -1) s.push(x);
    bool first = true;
    while (!s.empty()) {
        if (!first) cout << ' ';
        first = false;
        cout << s.top();
        s.pop();
    }
    cout << '\n';
}
```

### 2.4 Lab02-题目1：10 以内的后序波兰式求解（q139, 中等, 33 分）

**题意**：后缀表达式（操作数在前），数字 0..9，遇运算符弹两个数计算再压回。
- 输入 `23+6*` → `(2+3)*6 = 30`
- 非法 → 输出 `none`

**关键点**：
- 表达式是**一整行**，先 `getline` 再处理
- 数字字符 `'0'..'9'` 压栈
- 运算符 `+ - * /`：弹 b（后弹的右操作数），再弹 a，结果 `a op b` 压回
- 最终栈 size==1 才合法

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 自定义栈（顺序存储）
class MyStack {
    static const int MAXN = 105;
    int data[MAXN];
    int top;
public:
    MyStack(): top(-1) {}
    void push(int x) { data[++top] = x; }
    int pop()        { return data[top--]; }
    int peek() const { return data[top]; }
    bool empty() const { return top == -1; }
    int size() const { return top + 1; }
};

int main() {
    string s; getline(cin, s);
    MyStack st;       // 用 class MyStack，而不是 vector
    for (char c : s) {
        if (c >= '0' && c <= '9') st.push(c - '0');
        else if (c=='+'||c=='-'||c=='*'||c=='/') {
            if (st.size() < 2) { cout << "none\n"; return 0; }
            int b = st.pop();             // 后弹的右操作数
            int a = st.pop();
            int r = 0;
            if (c=='+') r = a + b;
            else if (c=='-') r = a - b;
            else if (c=='*') r = a * b;
            else r = a / b;               // 整数除
            st.push(r);
        }
    }
    if (st.size() == 1) cout << st.peek() << "\n";
    else                cout << "none\n";
}
```

### 2.5 Lab02-题目2：出入栈序列判断（q141, 中等, 33 分）

**题意**：I 入栈，O 出栈；初态终态都空。判断 n 个序列是否合法。
- 思路：模拟一遍，维护栈大小 `sz`。
  - I → `sz++`
  - O → `sz--`（出栈前 `sz` 必须 > 0）
  - 结束：合法当且仅当 `sz == 0`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class MyStack {
    static const int MAXN = 105;
    int data[MAXN];
    int top;
public:
    MyStack(): top(-1) {}
    void push(int x) { data[++top] = x; }
    int pop() { return data[top--]; }
    int size() const { return top + 1; }
    bool empty() const { return top == -1; }
};

int main() {
    int n; cin >> n;
    while (n--) {
        string s; cin >> s;
        MyStack st;       // 用 class 模拟
        bool ok = true;
        for (char c : s) {
            if (c == 'I') st.push(1);            // I 入栈
            else {                                // O 出栈
                if (st.empty()) { ok = false; break; }
                st.pop();
            }
        }
        if (!st.empty()) ok = false;             // 终态非空 = 非法
        cout << (ok ? "true" : "false") << "\n";
    }
}
```

### 2.6 Lab02-题目3：程序代码中的括号匹配（q142, 中等, 34 分）

**题意**：判断三类括号 `() [] {}` 是否匹配；**注释不参与**。
- 多行注释 `/* ... */`
- 单行注释 `// ...`

**关键点**：
- 维护一个 bool `inBlock`（多行注释内）和 `inLine`（单行注释内）
- 遇到 `/*` 且不在注释中 → `inBlock = true`
- 遇到 `*/` 且 `inBlock` → `inBlock = false`
- `//` 触发 `inLine = true` 直到换行
- 字符计数 = 各类括号总数，匹配 = 栈最终空

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class MyStack {
    static const int MAXN = 2048;
    char data[MAXN];
    int top;
public:
    MyStack(): top(-1) {}
    void push(char c) { data[++top] = c; }
    char pop() { return data[top--]; }
    char peek() const { return data[top]; }
    bool empty() const { return top == -1; }
    bool nonEmpty() const { return top != -1; }
};

int main() {
    string all, line;
    // 读若干行直到 EOF
    bool first = true;
    while (getline(cin, line)) {
        if (!first) all += '\n';
        all += line;
        first = false;
    }

    MyStack st;       // 用 class MyStack
    int cnt = 0;
    bool inBlock = false, inLine = false;
    bool ok = true;
    for (size_t i = 0; i < all.size(); i++) {
        char c = all[i], nxt = (i+1<all.size()? all[i+1]:'\0');
        if (inLine) { if (c=='\n') inLine = false; continue; }
        if (inBlock) { if (c=='*' && nxt=='/') { inBlock = false; i++; } continue; }
        if (c=='/' && nxt=='/') { inLine = true; i++; continue; }
        if (c=='/' && nxt=='*') { inBlock = true; i++; continue; }
        if (c=='('||c=='['||c=='{') { st.push(c); cnt++; }
        else if (c==')'||c==']'||c=='}') {
            cnt++;
            char m = (c==')')?'(':(c==']')?'[':'{';
            if (st.empty() || st.pop() != m) ok = false;
        }
    }
    if (st.nonEmpty()) ok = false;
    cout << cnt << ' ' << (ok ? "yes" : "no") << "\n";
}
```

### 2.7 Lab20-题目1：中序波兰式求解（q143, 简单, 100 分）

**题意**：中缀表达式求值，操作符 `+ - * /`，含括号；可能含浮点、负数。整数直接输出；浮点保留 3 位小数。

**关键点**：
- **双栈法**（操作数栈 + 操作符栈）是最稳的写法
- 优先级：`(` < `+ -` < `* /`
- 遇到 `(` 入栈；遇到 `)` 弹到 `(`；数字（含负数、小数）压栈
- 优先级比较：当前 op ≤ 栈顶 op 时，先计算栈顶

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 自定义栈模板
template <class T>
class MyStack {
    static const int MAXN = 1024;
    T data[MAXN];
    int top;
public:
    MyStack(): top(-1) {}
    void push(T x) { data[++top] = x; }
    T pop()        { return data[top--]; }
    T peek() const { return data[top]; }
    bool empty() const { return top == -1; }
    int size() const { return top + 1; }
};

int pri(char c) {
    if (c == '+' || c == '-') return 1;
    if (c == '*' || c == '/') return 2;
    return 0;
}

void calc(MyStack<double>& num, char op) {
    double b = num.pop();
    double a = num.pop();
    double r = 0;
    if (op=='+') r=a+b; else if (op=='-') r=a-b;
    else if (op=='*') r=a*b; else r=a/b;
    num.push(r);
}

int main() {
    string s; getline(cin, s);
    MyStack<double> num;       // 操作数栈
    MyStack<char>   op;        // 操作符栈
    int n = s.size();
    for (int i = 0; i < n; ) {
        char c = s[i];
        if (c==' ' || c=='\t') { i++; continue; }
        if (c=='(') { op.push(c); i++; }
        else if (c==')') {
            while (!op.empty() && op.peek()!='(') calc(num, op.pop());
            if (!op.empty()) op.pop();   // 弹出 (
            i++;
        }
        else if (c=='+'||c=='-'||c=='*'||c=='/') {
            // 一元负号：表达式开头 / 前一个 ( / 运算符后
            if (c=='-' && (i==0 || s[i-1]=='(' || strchr("+-*/", s[i-1]))) {
                num.push(0);   // 把 -x 看成 0-x
            }
            while (!op.empty() && op.peek()!='(' && pri(op.peek()) >= pri(c))
                calc(num, op.pop());
            op.push(c); i++;
        }
        else { // 数字
            int j = i;
            while (j<n && (isdigit(s[j]) || s[j]=='.')) j++;
            num.push(stod(s.substr(i, j-i)));
            i = j;
        }
    }
    while (!op.empty()) calc(num, op.pop());
    double ans = num.peek();
    if (ans == (long long)ans) cout << (long long)ans << "\n";
    else                        printf("%.3f\n", ans);
}
```

---


## 第 3 章 队列

### 章节速查

**核心主题**：FIFO 线性表 + 队列的应用

| 概念 | 关键点 |
|---|---|
| 顺序队列 | 假溢出 → 用循环队列 |
| 循环队列 | 队空 `front==rear`，队满 `(rear+1)%cap==front`（牺牲一个位置） |
| 双端队列 | 头尾都可进出 |
| 优先队列 | 出队按优先级（堆实现） |

**复杂度**：所有操作 O(1)，优先队列 O(logn)

**Lab 注意事项**：
- 环形队列：模运算 `(index+1)%cap`、牺牲一个位置区分空/满
- 字符串平衡：左右字符种类分别计数
- 双端队列：自定义实现 push_front / push_back / pop_front / pop_back
- 团队队列：两层队列——团队队列 + 成员队列

### 本章 Lab 代码（完整）

### 3.2 Lab24：环形队列（q146, 中等, 100 分）

**题意**：实现 `CirQueue`（顺序环形），用其将输入整数**顺序输出**。

**关键点**：
- 用 `count` 变量记录元素个数（比牺牲存储格更直观）
- 容量 105

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class CirQueue {
    static const int MAXN = 105;
    int data[MAXN];
    int frontIdx, rearIdx, cnt;
public:
    CirQueue(): frontIdx(0), rearIdx(-1), cnt(0) {}
    bool empty() const { return cnt == 0; }
    bool full()  const { return cnt == MAXN; }
    int  size()  const { return cnt; }
    void clear() { frontIdx=0; rearIdx=-1; cnt=0; }
    void enqueue(int x) { rearIdx = (rearIdx+1)%MAXN; data[rearIdx] = x; cnt++; }
    void dequeue()      { frontIdx = (frontIdx+1)%MAXN; cnt--; }
    int  front() const  { return data[frontIdx]; }
};

int main() {
    CirQueue q;
    int x; while (cin >> x && x != -1) q.enqueue(x);
    bool first = true;
    while (!q.empty()) {
        if (!first) cout << ' ';
        first = false;
        cout << q.front();
        q.dequeue();
    }
    cout << '\n';
}
```

### 3.3 Lab03-题目1：字符串的平衡性分析（q148, 中等, 33 分）

**题意**：读一行，找冒号 `:`，比较两侧：
- N 无冒号
- L 左侧长
- R 右侧长
- D 等长但不同
- S 完全相同

**关键点**：
- `find(':')`；找不到 → N
- 比较 `substr(0, p)` 与 `substr(p+1)`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n; cin.ignore();        // 吃掉 n 后的换行
    while (n--) {
        string s; getline(cin, s);
        size_t p = s.find(':');
        if (p == string::npos) { cout << "N\n"; continue; }
        string L = s.substr(0, p), R = s.substr(p+1);
        char ans;
        if (L == R) ans = 'S';
        else if (L.size() > R.size()) ans = 'L';
        else if (L.size() < R.size()) ans = 'R';
        else ans = 'D';
        cout << ans << "\n";
    }
}
```

### 3.4 Lab03-题目2：双端队列（q147, 中等, 33 分）

**题意**：实现 Deque，存最近 n 条操作记录（n=1..50），按"由近到远"输出（即倒序）。
- 历史上传超 n 条 → 只留最近 n 条
- 总数小于 n → 全输出

**关键点**：
- 维护 `vector<char>` 容器
- 入队（队尾）：`push_back`；满了就 `erase(begin())` 删最老的；或者用循环 buffer
- 输出：从 `back()` 倒着遍历到 `front()`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 自定义双端队列（顺序存储 + 模运算）
class MyDeque {
    static const int MAXN = 105;
    char data[MAXN];
    int head, tail, cnt;       // head 出队端，tail 入队端
public:
    MyDeque(): head(0), tail(-1), cnt(0) {}
    bool empty() const { return cnt == 0; }
    bool full()  const { return cnt == MAXN; }
    int  size()  const { return cnt; }
    void push_back(char c) { tail = (tail+1)%MAXN; data[tail] = c; cnt++; }
    void pop_front()      { head = (head+1)%MAXN; cnt--; }
    char front() const { return data[head]; }
    char back()  const { return data[tail]; }
};

int main() {
    int n; cin >> n;
    MyDeque dq;             // 用 class MyDeque
    char c;
    while (cin >> c) dq.push_back(c);
    while (dq.size() > n) dq.pop_front();   // 只留最近 n 条
    // 把内容存到数组再倒序输出（环形队列不便于直接倒序遍历）
    char buf[105];
    int len = 0;
    while (!dq.empty()) { buf[len++] = dq.front(); dq.pop_front(); }
    bool first = true;
    for (int i = len-1; i >= 0; i--) {
        if (!first) cout << ' ';
        first = false; cout << buf[i];
    }
    cout << '\n';
}
```

### 3.5 Lab03-题目3：团队队列（q150, 中等, 34 分）

**题意**：ENQUEUE x y（团队 x，编号 y）入队；若有队友在队，新人站到该团队**最后一名**之后；否则站到总队尾。DEQUEUE 出队首。STOP 结束。

**关键点**：
- 数据结构：外层是"团队顺序队列"，内层是"每个团队的成员队列"
- 维护一个 `member_to_team` 哈希（或数组）把编号映射回团队
- 新人到来：
  - 在 `member_to_team` 查 y 所属团队 t（题目输入即给出 t，简化）
  - 团队 t 不在团队顺序中 → 加到队尾
  - 把 y 加到团队 t 自己的队列尾
- DEQUEUE：
  - 取团队顺序首团队 t，弹出其队首成员 m
  - 团队 t 空了 → 从团队顺序里删掉 t
- 输出 m

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 自定义队列（顺序循环）
class MyQueue {
    static const int MAXN = 1024;
    int data[MAXN];
    int head, tail, cnt;
public:
    MyQueue(): head(0), tail(-1), cnt(0) {}
    bool empty() const { return cnt == 0; }
    void push(int x) { tail = (tail+1)%MAXN; data[tail] = x; cnt++; }
    int front() const { return data[head]; }
    void pop() { head = (head+1)%MAXN; cnt--; }
};

// 团队队列 = 外层团队顺序 + 内层每个团队的成员队列
class TeamQueue {
    MyQueue teamOrder;                              // 团队进入顺序
    unordered_map<int, MyQueue> memberQueue;        // 团队 -> 成员队列
public:
    void enqueue(int team, int id) {
        if (memberQueue[team].empty()) teamOrder.push(team);
        memberQueue[team].push(id);
    }
    int dequeue() {
        int t = teamOrder.front();
        int m = memberQueue[t].front();
        memberQueue[t].pop();
        if (memberQueue[t].empty()) teamOrder.pop();
        return m;
    }
};

int main() {
    int t, kase = 1;
    while (cin >> t && t) {
        cout << "Scenario #" << kase++ << "\n";
        TeamQueue tq;
        string op;
        while (cin >> op) {
            if (op == "STOP") break;
            if (op == "ENQUEUE") {
                int x, y; cin >> x >> y;
                tq.enqueue(x, y);
            } else if (op == "DEQUEUE") {
                cout << tq.dequeue() << "\n";
            }
        }
        cout << "\n";
    }
}
```

---


## 第 4 章 链式存储的栈与队列（含循环链表）

### 章节速查

**核心主题**：链式存储的栈与队列 + 循环链表 + 多项式

| 概念 | 关键点 |
|---|---|
| 链栈 | 头插法，top 在头部 |
| 链队列 | 头节点 + 尾指针 |
| 循环链表 | 尾节点的 next 指向头节点 |
| 多项式 | 链式存储（系数 + 指数 + next） |

**复杂度**：插入/删除 O(1)，查找 O(n)

**Lab 注意事项**：
- 约瑟夫问题：循环链表模拟，或**数学递推** `f(1)=0, f(n)=(f(n-1)+m)%n`
- 多项式运算：链表按指数排序，加减乘需要合并同类项

### 本章 Lab 代码（完整）

### 4.2 Lab06-题目1：链式存储的队列（q159, 困难, 50 分）

**题意**：实现 `MyLinkedQueue`（链式），用其将输入整数**顺序输出**。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

struct QNode { int val; QNode* next; QNode(int v): val(v), next(nullptr) {} };

class MyLinkedQueue {
    QNode *head, *tail;     // head 出队端，tail 入队端
    int cnt;
public:
    MyLinkedQueue(): head(nullptr), tail(nullptr), cnt(0) {}
    ~MyLinkedQueue() { while (head) { QNode* t = head; head = head->next; delete t; } }
    bool empty() const { return cnt == 0; }
    bool full()  const { return false; }
    int  size()  const { return cnt; }
    void clear() { this->~MyLinkedQueue(); head=tail=nullptr; cnt=0; }
    void enqueue(int x) {
        QNode* n = new QNode(x);
        if (!tail) head = tail = n;
        else { tail->next = n; tail = n; }
        cnt++;
    }
    void dequeue() {
        QNode* t = head; head = head->next;
        if (!head) tail = nullptr;
        delete t; cnt--;
    }
    int front() const { return head->val; }
};

int main() {
    MyLinkedQueue q;
    int x; while (cin >> x && x != -1) q.enqueue(x);
    bool first = true;
    while (!q.empty()) {
        if (!first) cout << ' ';
        first = false;
        cout << q.front();
        q.dequeue();
    }
    cout << '\n';
}
```

### 4.3 Lab04-题目3：约瑟夫问题（q155, 中等, 20 分）

**题意**：n 个人（2..60）围圈，从 1 报数到 m 出列；n=8, m=3 → 输出 7。

**关键点**：
- 循环单链表（尾→头）
- `cur` 每次前进 m-1 步后删除 next
- 终止条件：链表剩 1 个节点

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 循环链表节点
class CNode {
public:
    int id;
    CNode* next;
    CNode(int i): id(i), next(nullptr) {}
};

// 循环链表封装约瑟夫问题
class JosephusCircle {
    CNode* cur;       // 当前报数者
public:
    JosephusCircle(int n) {
        cur = new CNode(1);
        CNode* pre = cur;
        for (int i = 2; i <= n; i++) {
            pre->next = new CNode(i);
            pre = pre->next;
        }
        pre->next = cur;      // 成环
    }
    ~JosephusCircle() {
        // 释放
        CNode* p = cur->next;
        while (p != cur) { CNode* t = p; p = p->next; delete t; }
        delete cur;
    }
    // 报 m 出列，cur 前进 m-1 步，删除 next
    void eliminate(int m) {
        for (int k = 1; k < m; k++) cur = cur->next;
        CNode* del = cur->next;   // cur 报 m-1，del 报 m（出列）
        cur->next = del->next;
        if (del == cur) cur = nullptr;   // 删完只剩 cur 自己
        else            cur = del->next;
        delete del;
    }
    bool single() const { return cur && cur->next == cur; }
    int  winner() const { return cur ? cur->id : -1; }
};

int main() {
    int n, m; cin >> n >> m;
    JosephusCircle jc(n);
    while (!jc.single()) {
        jc.eliminate(m);
    }
    cout << jc.winner() << "\n";
}
```

### 4.4 Lab06-题目2：多项式的运算（q160, 中等, 50 分）

**题意**：多项式按指数降序输入，系数 + 指数成对，遇 `0 0` 结束。运算符 `+ - *`（乘法选做）。
- 输出 `5X^2` 这种格式，常数项写 `5`

**关键点**：
- 数据结构：链表（每项 `{coef, exp, next}`），按指数**降序**
- 加/减：双指针归并（指数大者先入）；相等指数则合并系数（减法取负）；系数为 0 则跳过
- 乘：每个 A 项 × 整个 B → 多项式，再加到结果中（外层加法用前面那个函数）
- 输出注意：
  - 第一项不带 `+` 号
  - 系数为 ±1 且指数 >0 时省略 1
  - 指数 = 0 不写 `X^0`；指数 = 1 写 `X`；否则 `X^e`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 多项式项（链表节点）
class Term {
public:
    int coef, exp;
    Term* next;
    Term(int c, int e): coef(c), exp(e), next(nullptr) {}
};

// 多项式（按指数降序）
class Polynomial {
    Term* head;       // 哑节点
    void copyFrom(const Polynomial& other);
public:
    Polynomial(): head(new Term(0,0)) { head->next = nullptr; }
    Polynomial(const Polynomial& other) { copyFrom(other); }
    ~Polynomial() {
        while (head) { Term* t = head; head = head->next; delete t; }
    }
    Polynomial& operator=(const Polynomial& other) {
        if (this != &other) {
            while (head) { Term* t = head; head = head->next; delete t; }
            head = new Term(0,0); head->next = nullptr;
            copyFrom(other);
        }
        return *this;
    }
    void read() {
        Term* tail = head;
        while (true) {
            int c, e; cin >> c >> e;
            if (c == 0 && e == 0) break;
            tail->next = new Term(c, e); tail = tail->next;
        }
        tail->next = nullptr;
    }
    void print() const {
        if (!head->next) { cout << "0\n"; return; }
        bool first = true;
        for (Term* p = head->next; p; p = p->next) {
            int c = p->coef, e = p->exp;
            if (!first) cout << (c >= 0 ? "+" : "");
            if (e == 0) cout << c;
            else {
                if (c == 1) cout << "X";
                else if (c == -1) cout << "-X";
                else cout << c << "X";
                if (e != 1) cout << "^" << e;
            }
            first = false;
        }
        cout << "\n";
    }
    // 加法：a + sign*b（sign=+1 加 / -1 减）
    static Polynomial add(const Polynomial& a, const Polynomial& b, int sign = 1) {
        Polynomial r;
        Term *ta = a.head->next, *tb = b.head->next, *tail = r.head;
        while (ta && tb) {
            if (ta->exp > tb->exp) {
                tail->next = new Term(ta->coef, ta->exp); tail = tail->next; ta = ta->next;
            } else if (ta->exp < tb->exp) {
                tail->next = new Term(tb->coef * sign, tb->exp); tail = tail->next; tb = tb->next;
            } else {
                int c = ta->coef + tb->coef * sign;
                if (c != 0) { tail->next = new Term(c, ta->exp); tail = tail->next; }
                ta = ta->next; tb = tb->next;
            }
        }
        while (ta) { tail->next = new Term(ta->coef, ta->exp); tail = tail->next; ta = ta->next; }
        while (tb) { tail->next = new Term(tb->coef * sign, tb->exp); tail = tail->next; tb = tb->next; }
        tail->next = nullptr;
        return r;
    }
    // 乘法：每个 A 项 × 整个 B 累加
    static Polynomial mul(const Polynomial& a, const Polynomial& b) {
        Polynomial res;
        for (Term* pa = a.head->next; pa; pa = pa->next) {
            Polynomial row;
            Term* tail = row.head;
            for (Term* pb = b.head->next; pb; pb = pb->next) {
                tail->next = new Term(pa->coef * pb->coef, pa->exp + pb->exp);
                tail = tail->next;
            }
            tail->next = nullptr;
            Polynomial tmp = add(res, row, 1);
            res = tmp;
        }
        return res;
    }
};

void Polynomial::copyFrom(const Polynomial& other) {
    head = new Term(0,0); head->next = nullptr;
    Term* tail = head;
    for (Term* p = other.head->next; p; p = p->next) {
        tail->next = new Term(p->coef, p->exp);
        tail = tail->next;
    }
    tail->next = nullptr;
}

int main() {
    char op; cin >> op;
    Polynomial p1, p2;
    p1.read(); p2.read();
    Polynomial res;
    if (op == '+')      res = Polynomial::add(p1, p2,  1);
    else if (op == '-') res = Polynomial::add(p1, p2, -1);
    else if (op == '*') res = Polynomial::mul(p1, p2);
    res.print();
}
```

---


## 第 5 章 递归

### 章节速查

**核心主题**：递归的本质与设计

| 概念 | 关键点 |
|---|---|
| 递归三要素 | 终止条件 + 递归公式 + 缩小规模 |
| 调用栈 | 系统栈保存每层参数 + 局部变量 |
| 尾递归 | 最后一步是递归调用，可优化为迭代 |
| 递归 vs 回溯 | 回溯 = 递归 + 状态恢复 |

**复杂度**：递归深度 = 调用栈深度；空间 O(深度)

**Lab 注意事项**：
- 最小求和：从 n 个数选 k 个使和最小 → 排序后取前 k 个
- 二进制转十进制：`f(s, i) = f(s, i+1)*2 + (s[i]-'0')`
- 全排列：固定当前位置，递归排列剩余，最后交换回来
- 迷宫 BFS：用队列维护待探索格子，dist[][] 记录步数

### 本章 Lab 代码（完整）

### 5.2 Lab25-题目1：递归求最小求和（q103, 困难, 50 分）

**题意**：用递归实现 `Min(arr, n)` 和 `Sum(arr, n)`。

**关键点**：
- `Sum(arr, n) = Sum(arr, n-1) + arr[n-1]`；`Sum(arr, 1) = arr[0]`
- `Min(arr, n) = min(Min(arr, n-1), arr[n-1])`；`Min(arr, 1) = arr[0]`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int Sum(int arr[], int n) { return n == 1 ? arr[0] : Sum(arr, n-1) + arr[n-1]; }
int Min(int arr[], int n) { return n == 1 ? arr[0] : min(Min(arr, n-1), arr[n-1]); }

int main() {
    int n; cin >> n;
    int* a = new int[n];
    for (int i = 0; i < n; i++) cin >> a[i];
    cout << Min(a, n) << ' ' << Sum(a, n) << "\n";
    delete[] a;
}
```

### 5.3 Lab25-题目2：二进制转十进制（q161, 中等, 50 分）

**题意**：递归把二进制串转为十进制。

**关键点**：
- `bin(s) = bin(s[0..n-2]) * 2 + s[n-1]`
- 终止：空串 → 0

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

long long bin(string s) {
    if (s.empty()) return 0;
    return bin(s.substr(0, s.size()-1)) * 2 + (s.back() - '0');
}
int main() {
    string s; cin >> s;
    cout << bin(s) << "\n";
}
```

### 5.4 Lab07-题目1：n 个数的全排列（q163, 中等, 50 分）

**题意**：n ≤ 20，输出 1..n 的全排列（超过 10 个只输出前 10），并输出总排列数。

**关键点**：
- 回溯 + `used[]` 标记
- `dfs(k)`：已选 k 个，再选第 k+1 个
- `n!` 超过 10 之后停止输出（但仍可继续计算）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int n, printed = 0;
vector<int> a;
vector<bool> used;

void dfs(int k) {
    if (k == n) {
        if (printed < 10) {
            cout << (printed+1) << ":";
            for (int i = 0; i < n; i++) cout << (i?" ":"") << a[i];
            cout << "\n";
            printed++;
        }
        return;
    }
    for (int i = 1; i <= n; i++) {
        if (!used[i]) {
            used[i] = true; a.push_back(i);
            dfs(k+1);
            used[i] = false; a.pop_back();
        }
    }
}

int main() {
    cin >> n;
    a.assign(n, 0);
    used.assign(n+1, false);
    dfs(0);
    long long total = 1;
    for (int i = 2; i <= n; i++) total *= i;
    cout << total << "\n";
}
```

### 5.5 Lab07-题目2：A Maze Problem（q164, 中等, 50 分）

**题意**：12×12 迷宫，O/W/E，3 个起点，问到出口的最短路径长度（起终点都算入），不可达输出 -1。

**关键点**：
- **BFS 求最短路**（不是 DFS，DFS 求的是任意路径）
- `dirs = {(1,0),(-1,0),(0,1),(0,-1)}`
- `O=0, W=1, E=2`；E 是终点
- BFS 起点 = (x, y)（注意题目说 x 是纵坐标，y 是横坐标）
- 距离 = `dist[x][y]`，终点 `dist[ex][ey]`
- 起始结束都算：长度 = 步数 + 1

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    const int N = 12;
    int grid[N][N];
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++) {
            char c; cin >> c;
            grid[i][j] = (c=='W'?1:(c=='E'?2:0));
        }
    // 找 E
    int ex=-1, ey=-1;
    for (int i=0;i<N;i++) for (int j=0;j<N;j++) if (grid[i][j]==2){ ex=i; ey=j; }

    // 3 个起点
    for (int k = 0; k < 3; k++) {
        int x, y; cin >> x >> y;     // (x,y) = (row, col)
        if (k) cout << ' ';
        if (grid[x][y] == 1) { cout << -1; continue; }

        int dist[N][N];
        for (int i=0;i<N;i++) for (int j=0;j<N;j++) dist[i][j] = -1;
        queue<pair<int,int>> q;
        dist[x][y] = 0; q.push({x,y});
        int d4[4][2] = {{-1,0},{1,0},{0,-1},{0,1}};
        while (!q.empty()) {
            auto [i,j] = q.front(); q.pop();
            for (auto& d : d4) {
                int ni = i+d[0], nj = j+d[1];
                if (ni<0||ni>=N||nj<0||nj>=N) continue;
                if (grid[ni][nj]==1) continue;
                if (dist[ni][nj]!=-1) continue;
                dist[ni][nj] = dist[i][j] + 1;
                q.push({ni,nj});
            }
        }
        if (dist[ex][ey] == -1) cout << -1;
        else cout << dist[ex][ey] + 1;     // 起终点都算
    }
    cout << "\n";
}
```

---


## 第 6 章 列表 List

### 章节速查

**核心主题**：顺序表 + 链表 + 快慢指针 + 大数运算

| 概念 | 关键点 |
|---|---|
| 顺序表 | 连续内存，O(1) 随机访问 |
| 链表 | 离散内存，O(1) 插入删除 |
| 快慢指针 | 找中点 / 判环 / 找倒数第 k |
| 链表反转 | 三指针 prev / cur / nxt |
| 大数运算 | 用数组每位存一位数字 |

**复杂度对比**：

| 操作 | 顺序表 | 链表 |
|---|---|---|
| 随机访问 | O(1) | O(n) |
| 插入/删除头部 | O(n) | O(1) |
| 插入/删除尾部 | O(1) | O(1) / O(n)（无尾指针）|
| 插入/删除中间 | O(n) | O(1)（已知位置）|

**Lab 注意事项**：
- 单链表创建：头插法 / 尾插法
- 链表反转：三指针迭代 / 递归
- 大数加法：从低位到高位逐位相加 + 进位
- 双链表：每个节点多一个 prev 指针

### 本章 Lab 代码（完整）

### 6.2 Lab22：单链表的创建与输出（q151, 困难, 100 分）

**题意**：读 n 个整数建单链表（堆内存），输出**和**。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 单链表节点
class Node {
public:
    int v;
    Node* next;
    Node(int x): v(x), next(nullptr) {}
};

// 单链表（带头尾指针）
class LinkedList {
    Node* head;
    Node* tail;
    int sz;
public:
    LinkedList(): head(nullptr), tail(nullptr), sz(0) {}
    ~LinkedList() { while (head) { Node* t = head; head = head->next; delete t; } }
    void append(int x) {
        Node* p = new Node(x);
        if (!head) head = tail = p;
        else { tail->next = p; tail = p; }
        sz++;
    }
    int size() const { return sz; }
    // 遍历求和
    long long sum() const {
        long long s = 0;
        for (Node* p = head; p; p = p->next) s += p->v;
        return s;
    }
};

int main() {
    int n; cin >> n;
    LinkedList L;       // 用 class LinkedList
    for (int i = 0; i < n; i++) { int x; cin >> x; L.append(x); }
    cout << L.sum() << "\n";
}
```

### 6.3 Lab04-题目1：单链表指定值插入（q153, 困难, 20 分）

**题意**：在单链表中所有值 x 之后插入 y，输出**插入位置**（1-indexed 位置），插入后链表变化不影响后续位置判断。

**关键点**：
- **关键观察**：题目要的是"插入后 y 的下标位置"，且要在**原始链表**中标定
- 扫描原链表，记录所有 x 出现的位置 i（1-indexed），则在 i+1 处插入 y
- 所以"位置" = `i + 1`（1-indexed）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    vector<int> a(n+1);
    for (int i = 1; i <= n; i++) cin >> a[i];
    int x, y; cin >> x >> y;
    bool first = true;
    for (int i = 1; i <= n; i++) {
        if (a[i] == x) {
            if (!first) cout << ' ';
            first = false;
            cout << i + 1;
        }
    }
    cout << "\n";
}
```

### 6.4 Lab04-题目2：删除单链表指定值（q154, 困难, 20 分）

**题意**：删除所有值 = x 的节点，输出剩余链表（空格分隔）。

**C++ 模板**（数组模拟即可）：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a;
    for (int i = 0; i < n; i++) { int x; cin >> x; a.push_back(x); }
    int x; cin >> x;
    bool first = true;
    for (int v : a) if (v != x) {
        if (!first) cout << ' ';
        first = false; cout << v;
    }
    cout << "\n";
}
```

### 6.5 Lab04-题目4：链表置逆（q156, 中等, 20 分）

**题意**：一趟遍历把单链表所有链接方向逆转。

**关键点**：
- 三指针：`pre = nullptr, cur = head`
- 循环：保存 `nxt = cur->next`；`cur->next = pre`；`pre = cur`；`cur = nxt`
- 退出后 `pre` 即新头

**C++ 模板**（数组版更简洁）：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    reverse(a.begin(), a.end());
    for (int i = 0; i < n; i++) {
        if (i) cout << ' ';
        cout << a[i];
    }
    cout << "\n";
}
```

### 6.6 Lab04-题目5：删除链表重复元素（q157, 中等, 20 分）

**题意**：保留每个值第一次出现的节点。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a;
    set<int> seen;
    for (int i = 0; i < n; i++) {
        int x; cin >> x;
        if (!seen.count(x)) { seen.insert(x); a.push_back(x); }
    }
    for (int i = 0; i < (int)a.size(); i++) {
        if (i) cout << ' ';
        cout << a[i];
    }
    cout << "\n";
}
```

### 6.7 Lab08-题目1：List 的顺序实现（q165, 困难, 50 分）

**题意**：实现通用顺序线性表 `MyList`，支持 Insert/Remove/Retrieve/Replace/Traverse/Size/Full；用其将输入序列顺序输出。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class MyList {
    static const int CAP = 105;
    int data[CAP];
    int sz;
public:
    MyList(): sz(0) {}
    bool full() const { return sz == CAP; }
    int  size() const { return sz; }
    bool insert(int pos, int x) {       // 0 <= pos <= sz，在 pos 前插入
        if (full() || pos < 0 || pos > sz) return false;
        for (int i = sz; i > pos; i--) data[i] = data[i-1];
        data[pos] = x; sz++; return true;
    }
    bool remove(int pos) {              // 0 <= pos < sz
        if (pos < 0 || pos >= sz) return false;
        for (int i = pos; i < sz-1; i++) data[i] = data[i+1];
        sz--; return true;
    }
    bool retrieve(int pos, int& x) const {
        if (pos < 0 || pos >= sz) return false;
        x = data[pos]; return true;
    }
    bool replace(int pos, int x) {
        if (pos < 0 || pos >= sz) return false;
        data[pos] = x; return true;
    }
    void traverse() const {
        for (int i = 0; i < sz; i++) cout << (i?" ":"") << data[i];
        cout << "\n";
    }
};

int main() {
    MyList L;
    int x; while (cin >> x && x != -1) L.insert(L.size(), x);
    L.traverse();
}
```

### 6.8 Lab08-题目2：List 的单链表实现（q168, 困难, 50 分）

**题意**：单链表实现通用 List（含最后访问位置记录），N 次插入、M 次删除、P 次替换；输出最终序列。

**关键点**：
- 哑节点 + 头插/尾插
- 最后访问位置 = 索引 → 节点指针 + 索引（更新时机：每次 insert/remove/replace 后更新）
- 输入：4 行；操作均以 `-1` 或 `-1 -1` 结束

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

struct LNode { int v; LNode* next; LNode(int x): v(x), next(nullptr) {} };

class MyList {
    LNode* head;      // dummy
    int sz;
    int lastPos;      // 最后访问位置（0-indexed）
    LNode* lastPtr;   // 指向 lastPos 节点
    void updateLast(int pos) {
        lastPos = pos;
        LNode* p = head->next;
        for (int i = 0; i < pos; i++) p = p->next;
        lastPtr = p;
    }
public:
    MyList(): head(new LNode(0)), sz(0), lastPos(-1), lastPtr(nullptr) {}
    ~MyList() { while (head) { LNode* t = head; head = head->next; delete t; } }
    int size() const { return sz; }
    bool insert(int pos, int x) {
        if (pos < 0 || pos > sz) return false;
        LNode* pre = head;
        for (int i = 0; i < pos; i++) pre = pre->next;
        LNode* n = new LNode(x);
        n->next = pre->next; pre->next = n; sz++;
        updateLast(pos);
        return true;
    }
    bool remove(int pos) {
        if (pos < 0 || pos >= sz) return false;
        LNode* pre = head;
        for (int i = 0; i < pos; i++) pre = pre->next;
        LNode* t = pre->next; pre->next = t->next; delete t; sz--;
        if (pos < sz) updateLast(pos); else { lastPos = -1; lastPtr = nullptr; }
        return true;
    }
    bool replace(int pos, int x) {
        if (pos < 0 || pos >= sz) return false;
        LNode* p = head->next;
        for (int i = 0; i < pos; i++) p = p->next;
        p->v = x; updateLast(pos); return true;
    }
    void traverse() const {
        LNode* p = head->next;
        bool first = true;
        while (p) {
            if (!first) cout << ' ';
            first = false; cout << p->v;
            p = p->next;
        }
        cout << "\n";
    }
};

int main() {
    MyList L;
    int x;
    // 第 1 行：原序列
    while (cin >> x && x != -1) L.insert(L.size(), x);
    // 吃掉换行（如果有）
    // 第 2 行：插入
    while (cin >> x) {
        int a = x, b; cin >> b;
        if (a == -1 && b == -1) break;
        L.insert(a, b);
    }
    // 第 3 行：删除
    while (cin >> x) {
        if (x == -1) break;
        L.remove(x);
    }
    // 第 4 行：替换
    while (cin >> x) {
        int d = x, e; cin >> e;
        if (d == -1 && e == -1) break;
        L.replace(d, e);
    }
    L.traverse();
}
```

### 6.9 Lab09-题目1：List 的双链表实现（q169, 困难, 33 分）

**题意**：双链表实现 List，记录最后访问位置，输出正序和逆序。

**关键点**：
- 双链表每个节点 `{prev, next, val}`
- 维护 `head`（哑）和 `tail`
- Insert：在 pos 前插入；同步维护 `tail`
- Remove：删 pos 节点；tail 要更新
- 输出正序 + 逆序

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

struct DNode { int v; DNode *prev, *next; DNode(int x): v(x), prev(nullptr), next(nullptr) {} };

class MyList {
    DNode *head, *tail;     // 双向哑节点简化
    int sz;
    void link(DNode* a, DNode* b) { if (a) a->next = b; if (b) b->prev = a; }
public:
    MyList() {
        head = new DNode(0); tail = new DNode(0);
        link(head, tail); sz = 0;
    }
    ~MyList() {
        DNode* p = head;
        while (p) { DNode* t = p; p = p->next; delete t; }
    }
    int size() const { return sz; }
    bool insert(int pos, int x) {
        if (pos < 0 || pos > sz) return false;
        DNode* pre = head;
        for (int i = 0; i < pos; i++) pre = pre->next;
        DNode* n = new DNode(x);
        link(n, pre->next); link(pre, n); sz++; return true;
    }
    bool remove(int pos) {
        if (pos < 0 || pos >= sz) return false;
        DNode* p = head->next;
        for (int i = 0; i < pos; i++) p = p->next;
        link(p->prev, p->next); delete p; sz--; return true;
    }
    bool replace(int pos, int x) {
        if (pos < 0 || pos >= sz) return false;
        DNode* p = head->next;
        for (int i = 0; i < pos; i++) p = p->next;
        p->v = x; return true;
    }
    void traverse() const {
        DNode* p = head->next;
        bool first = true;
        while (p != tail) {
            if (!first) cout << ' ';
            first = false; cout << p->v;
            p = p->next;
        }
        cout << "\n";
    }
    void traverseRev() const {
        DNode* p = tail->prev;
        bool first = true;
        while (p != head) {
            if (!first) cout << ' ';
            first = false; cout << p->v;
            p = p->prev;
        }
        cout << "\n";
    }
};

int main() {
    MyList L;
    int x;
    while (cin >> x && x != -1) L.insert(L.size(), x);
    while (cin >> x) { int a=x,b; cin>>b; if (a==-1&&b==-1) break; L.insert(a,b); }
    while (cin >> x) { if (x==-1) break; L.remove(x); }
    while (cin >> x) { int d=x,e; cin>>e; if (d==-1&&e==-1) break; L.replace(d,e); }
    L.traverse();
    L.traverseRev();
}
```

### 6.10 Lab09-题目2：页面的相关系数（q167, 中等, 33 分）

**题意**：N 个 Web 页面有相关系数 Vi，输出最大相关系数的所有 URL（按输入顺序）。

**关键点**：用 MyList 装结构 `{url, vi}`；扫一遍找 max，再扫一遍输出所有等于 max 的（按顺序）。

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 页面节点
class Page {
public:
    string url;
    int v;
    Page* next;
    Page(string u, int x): url(u), v(x), next(nullptr) {}
};

// 用单链表实现 MyList
class MyPageList {
    Page* head;
    int sz;
public:
    MyPageList(): head(nullptr), sz(0) {}
    ~MyPageList() { while (head) { Page* t = head; head = head->next; delete t; } }
    void append(string u, int v) {
        Page* p = new Page(u, v);
        if (!head) { head = p; sz = 1; return; }
        Page* cur = head;
        while (cur->next) cur = cur->next;
        cur->next = p;
        sz++;
    }
    int findMax() const {
        int mx = 0;
        for (Page* p = head; p; p = p->next) if (p->v > mx) mx = p->v;
        return mx;
    }
    // 按输入顺序输出所有 v == max 的 url
    void printMax() const {
        int mx = findMax();
        for (Page* p = head; p; p = p->next)
            if (p->v == mx) cout << p->url << "\n";
    }
};

int main() {
    int N; cin >> N;
    MyPageList L;
    for (int i = 0; i < N; i++) { string u; int v; cin >> u >> v; L.append(u, v); }
    L.printMax();
}
```

### 6.11 Lab09-题目3：大数运算（q166, 简单, 34 分）

**题意**：实现 `MyBigInteger` 支持 `+ - *`（除法选做）。位数 ≤ 200。

**关键点**：
- 用 `vector<int>` 存十进制位，**低位在前**
- 加：从低位加，进位 carry
- 减：先比较长度（被减数 ≥ 减数），否则取负
- 乘：每位 × 另一个数 + 进位，O(n²) 够用
- 输出：去前导 0；若为 0 输出 `0`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 大整数类（十进制位，低位在前）
class MyBigInteger {
    vector<int> d;            // 低位在前
    void trim() { while (d.size()>1 && d.back()==0) d.pop_back(); }
public:
    MyBigInteger(long long x = 0) { while (x) { d.push_back(x%10); x/=10; } if (d.empty()) d.push_back(0); }
    MyBigInteger(const string& s) {
        d.clear();
        for (int i=(int)s.size()-1; i>=0; i--) d.push_back(s[i]-'0');
        trim();
    }
    bool isZero() const { return d.size()==1 && d[0]==0; }
    bool operator<(const MyBigInteger& o) const {
        if (d.size()!=o.d.size()) return d.size()<o.d.size();
        for (int i=(int)d.size()-1;i>=0;i--) if (d[i]!=o.d[i]) return d[i]<o.d[i];
        return false;
    }
    string toString() const {
        if (isZero()) return "0";
        string s; for (int i=(int)d.size()-1; i>=0; i--) s.push_back('0'+d[i]);
        return s;
    }
    // 静态成员：加
    static MyBigInteger add(const MyBigInteger& a, const MyBigInteger& b) {
        MyBigInteger r;
        r.d.assign(max(a.d.size(),b.d.size()), 0);
        int c = 0;
        for (size_t i = 0; i < r.d.size(); i++) {
            int s = c + (i<a.d.size()?a.d[i]:0) + (i<b.d.size()?b.d[i]:0);
            r.d[i] = s % 10; c = s / 10;
        }
        if (c) r.d.push_back(c);
        return r;
    }
    // 静态成员：减（要求 a >= b）
    static MyBigInteger sub(const MyBigInteger& a, const MyBigInteger& b) {
        MyBigInteger r = a;
        int c = 0;
        for (size_t i = 0; i < r.d.size(); i++) {
            int s = r.d[i] - (i<b.d.size()?b.d[i]:0) - c;
            if (s < 0) { s += 10; c = 1; } else c = 0;
            r.d[i] = s;
        }
        r.trim();
        return r;
    }
    // 静态成员：乘
    static MyBigInteger mul(const MyBigInteger& a, const MyBigInteger& b) {
        MyBigInteger r;
        r.d.assign(a.d.size()+b.d.size(), 0);
        for (size_t i=0; i<a.d.size(); i++)
            for (size_t j=0; j<b.d.size(); j++)
                r.d[i+j] += a.d[i]*b.d[j];
        for (size_t i=0; i<r.d.size(); i++) {
            if (r.d[i] >= 10) { r.d[i+1] += r.d[i]/10; r.d[i] %= 10; }
        }
        r.trim();
        return r;
    }
};

int main() {
    string s1, s2; char op;
    cin >> s1 >> s2 >> op;
    MyBigInteger A(s1), B(s2), R;
    if (op=='+') R = MyBigInteger::add(A,B);
    else if (op=='-') {
        if (A < B) { cout << "-"; R = MyBigInteger::sub(B, A); }
        else        R = MyBigInteger::sub(A, B);
    }
    else if (op=='*') R = MyBigInteger::mul(A, B);
    cout << R.toString() << "\n";
}
```

### 6.12 Lab10-题目1：String 的实现（q170, 中等, 100 分）

**题意**：用 `List<char>` 内部存储实现 String，支持比较、连接、子串查找、取子串；N 个串，输出子串查询结果 + 字典序排序。

**关键点**：
- 内部用 `vector<char>` 装字符
- 算长度、`==` `<` 比较：长度不等直接定，否则逐字符比
- 子串查找：朴素 O(nm) 够用
- 字典序：直接用 `<` 比较，sort

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class String {
    vector<char> s;
public:
    String() {}
    String(const char* p) { while (*p) s.push_back(*p++); }
    String(const string& t) : s(t.begin(), t.end()) {}
    int length() const { return (int)s.size(); }
    const char* c_str() const {
        static char buf[100005];   // 简化：非线程安全，Lab 够用
        for (size_t i = 0; i < s.size(); i++) buf[i] = s[i];
        buf[s.size()] = '\0';
        return buf;
    }
    String substr(int l, int r) const {   // [l, r)
        String t;
        for (int i = l; i < r; i++) t.s.push_back(s[i]);
        return t;
    }
    int find(const String& sub) const {
        int n = length(), m = sub.length();
        if (m == 0) return 0;
        for (int i = 0; i + m <= n; i++) {
            int j = 0;
            while (j < m && s[i+j] == sub.s[j]) j++;
            if (j == m) return i;
        }
        return -1;
    }
    bool operator==(const String& o) const { return s == o.s; }
    bool operator<(const String& o) const {
        size_t n = min(s.size(), o.s.size());
        for (size_t i = 0; i < n; i++) if (s[i] != o.s[i]) return s[i] < o.s[i];
        return s.size() < o.s.size();
    }
};

int main() {
    int N; cin >> N;
    vector<String> arr(N);
    for (int i = 0; i < N; i++) {
        string t; cin >> t; arr[i] = String(t);
    }
    // 第 3 行：查询（a b 对，判断 arr[a] 是否是 arr[b] 的子串）
    vector<int> ans;
    while (true) {
        int a, b; cin >> a >> b;
        if (a == -1 && b == -1) break;
        if (a < 0 || a >= N || b < 0 || b >= N) ans.push_back(-1);
        else ans.push_back(arr[b].find(arr[a]));
    }
    for (int i = 0; i < (int)ans.size(); i++) {
        if (i) cout << ' ';
        cout << ans[i];
    }
    cout << "\n";
    // 字典序
    vector<String> sorted = arr;
    sort(sorted.begin(), sorted.end());
    for (int i = 0; i < N; i++) {
        if (i) cout << ' ';
        cout << sorted[i].c_str();
    }
    cout << "\n";
}
```

---


## 第 7 章 查找

### 章节速查

**核心主题**：各种查找算法 + 二分查找变体

| 查找方法 | 时间复杂度 | 适用 |
|---|---|---|
| 顺序查找 | O(n) | 无序 |
| 二分查找 | O(logn) | 有序 |
| 插值查找 | O(loglogn) | 均匀分布 |
| 斐波那契查找 | O(logn) | 平均性能 |
| 分块查找 | O(√n) | 块内无序、块间有序 |
| 哈希查找 | O(1) 期望 | 见第 9 章 |
| BST/AVL | O(logn) | 见第 10 章 |

**二分查找 6 大变体（必背）**：
1. 找等于 target 的元素
2. 找第一个 ≥ target 的位置
3. 找第一个 > target 的位置
4. 找最后一个 ≤ target 的位置
5. 找最后一个 < target 的位置
6. 找等于 target 的最后一个位置

**Lab 注意事项**：
- 前向/后向最近邻：每个位置向左 / 向右找最近的较大值，单调栈
- 开方值：二分精度控制（`while hi-lo > 1e-k`）
- 二分查找左右边界：`while (lo < hi)` 统一风格，最后检查 `nums[lo] == target`

### 本章 Lab 代码（完整）

### 7.2 Lab11-题目1：前向和后向的最近邻值（q172, 中等, 33 分）

**题意**：双链表节点值均为正整数。
- **前向最近邻值**：节点 i 之前 < i 的最大值（不存在 → 0）
- **后向最近邻值**：节点 i 之后 > i 的最小值（不存在 → 0）

**关键点**：
- 暴力 O(n²) 即可（m < 1000，n = m）
- 前向：j 从 i-1 往 0 走，取 < a[i] 的最大值
- 后向：j 从 i+1 往 m-1 走，取 > a[i] 的最小值
- 注意：找"最大值"用 `>`，找"最小值"用 `<`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> a;
    int x; while (cin >> x && x != -1) a.push_back(x);
    int n = a.size();
    // 前向最近邻值
    for (int i = 0; i < n; i++) {
        int best = 0;
        for (int j = 0; j < i; j++) if (a[j] < a[i] && a[j] > best) best = a[j];
        if (i) cout << ' ';
        cout << best;
    }
    cout << "\n";
    // 后向最近邻值
    for (int i = 0; i < n; i++) {
        int best = 0;
        for (int j = i+1; j < n; j++) if (a[j] > a[i]) { if (!best || a[j] < best) best = a[j]; }
        if (i) cout << ' ';
        cout << best;
    }
    cout << "\n";
}
```

### 7.3 Lab11-题目2：求开方值（q176, 中等, 33 分）

**题意**：实现 `int sqrt(int x)`，返回整数部分。

**关键点**：
- **二分查找**，`lo=0, hi=min(x, 46340)`（避免 mid*mid 溢出 int；或用 long long）
- 求 `mid*mid <= x` 的最大 mid

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    long long x; cin >> x;
    long long lo = 0, hi = min(x, 100000LL);
    while (lo <= hi) {
        long long mid = (lo+hi)/2;
        if (mid*mid <= x) lo = mid+1;
        else hi = mid-1;
    }
    cout << hi << "\n";
}
```

### 7.4 Lab11-题目3：Start and End Position（q175, 中等, 34 分）

**题意**：升序数组找 target 第一次和最后一次出现的位置；不存在返回 `[-1,-1]`；要求 O(log n)。

**关键点**：
- 两次二分：找左边界、找右边界
- 左边界：找第一个 ≥ target
- 右边界：找第一个 > target，再 -1
- 找不到时左边界 = n（即 `lo == n`），需处理

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// 找第一个 >= target 的位置
int lowerBound(vector<int>& a, int target) {
    int lo=0, hi=a.size();
    while (lo < hi) {
        int mid = (lo+hi)/2;
        if (a[mid] < target) lo = mid+1;
        else hi = mid;
    }
    return lo;
}

int main() {
    int target; cin >> target;
    vector<int> a; int x;
    while (cin >> x && x != -1) a.push_back(x);
    int lo = lowerBound(a, target);
    if (lo == (int)a.size() || a[lo] != target) {
        cout << "[-1,-1]\n";
    } else {
        int hi = lowerBound(a, target+1) - 1;
        cout << "[" << lo << "," << hi << "]\n";
    }
}
```

### 7.5 Lab12-题目1：二分查找的实现（q178, 中等, 100 分）

**题意**：单词（key）按升序，查询 target，输出下标 + key + other（中文解释）；不存在输出 -1。

**关键点**：
- 数据结构：`vector<pair<string,string>>` 或自定义 Record
- 二分：`mid = (lo+hi)/2`
- 比较 `a[mid].first` 与 `target`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string target; cin >> target;
    vector<pair<string,string>> a;
    string k, v;
    while (cin >> k >> v) a.push_back({k, v});
    int lo = 0, hi = a.size() - 1, ans = -1;
    while (lo <= hi) {
        int mid = (lo+hi)/2;
        if (a[mid].first == target) { ans = mid; break; }
        else if (a[mid].first < target) lo = mid+1;
        else hi = mid-1;
    }
    if (ans == -1) cout << -1 << "\n";
    else cout << ans << "\n" << a[ans].first << " " << a[ans].second << "\n";
}
```

---


## 第 8 章 排序

### 章节速查

**核心主题**：十大排序算法

| 排序 | 时间 | 空间 | 稳定 | 适用 |
|---|---|---|---|---|
| 冒泡 | O(n²) | O(1) | ✅ | 教学 |
| 插入 | O(n²) | O(1) | ✅ | 基本有序 / 小数据 |
| 希尔 | O(n^1.3) | O(1) | ❌ | 中等规模 |
| 选择 | O(n²) | O(1) | ❌ | 交换少 |
| **堆排** | O(nlogn) | O(1) | ❌ | 原地排序、取 top-k |
| **归并** | O(nlogn) | O(n) | ✅ | 链表排序、外排 |
| **快排** | O(nlogn) | O(logn) | ❌ | 通用最快 |
| 计数 | O(n+k) | O(k) | ✅ | 范围小 |
| 基数 | O(d(n+k)) | O(k) | ✅ | 多关键字 |
| 桶 | O(n+k) | O(n+k) | ✅ | 均匀分布 |

**Lab 注意事项**：
- 插入排序链式实现：维护有序链表，遍历找到插入位置
- Largest Number：自定义排序（a+b > b+a 时 a 在前）
- 堆排：buildHeap 自底向上 siftDown，siftDown 公式 `max(2i+1, 2i+2)` 选大者
- 快排 partition：**课本用末元素作轴**，三数取中可避免最坏
- Sort Colors：单趟三路划分（0/1/2 分桶）

### 本章 Lab 代码（完整）

### 8.2 Lab13-题目1：插入排序的顺序实现（q180, 中等, 33 分）

**题意**：在顺序 List 上做插入排序，**输出有序部分长度为 n/2 时的中间结果**和最终结果。

**关键点**：
- 标准插入排序：把 a[i] 插入到已排序区 a[0..i-1] 中
- 维护 `len`：已排序区长度
- 每次插入后，如果 `len == n/2` 立刻输出当前数组

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    bool printed = false;
    for (int i = 1; i < n; i++) {
        int key = a[i], j = i - 1;
        while (j >= 0 && a[j] > key) { a[j+1] = a[j]; j--; }
        a[j+1] = key;
        if (!printed && i+1 == n/2) {
            for (int k = 0; k < n; k++) cout << (k?" ":"") << a[k];
            cout << "\n";
            printed = true;
        }
    }
    for (int k = 0; k < n; k++) cout << (k?" ":"") << a[k];
    cout << "\n";
}
```

### 8.3 Lab13-题目2：插入排序的链式实现（q188, 中等, 33 分）

**题意**：链式 List 上做插入排序；同样输出 `n/2` 长度中间结果 + 最终结果。

**关键点**：
- 维护一个**已排序链表**，逐个节点拿出来**插入**到合适位置
- 哑节点简化头插
- 长度 = n/2 时输出整张链表

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
struct LNode { int v; LNode* next; LNode(int x): v(x), next(nullptr) {} };

void printList(LNode* head) {
    bool first = true;
    while (head) {
        if (!first) cout << ' ';
        first = false; cout << head->v;
        head = head->next;
    }
    cout << "\n";
}

int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    LNode* sorted = new LNode(a[0]);
    bool printed = false;
    for (int i = 1; i < n; i++) {
        LNode* nw = new LNode(a[i]);
        LNode dummy(0); dummy.next = sorted;
        LNode* p = &dummy;
        while (p->next && p->next->v < nw->v) p = p->next;
        nw->next = p->next; p->next = nw;
        sorted = dummy.next;
        if (!printed && i+1 == n/2) {
            printList(sorted);
            printed = true;
        }
    }
    printList(sorted);
    // 释放
    while (sorted) { LNode* t = sorted; sorted = sorted->next; delete t; }
}
```

### 8.4 Lab13-题目3：Largest Number（q184, 中等, 34 分）

**题意**：把 n 个非负数排成最大数（如 `[3,30,34,5,9] → "9534330"`）。

**关键点**：
- 自定义比较：拼成 `s1+s2` 和 `s2+s1`，按字典序**大者**在前
- 用 `sort` + `lambda`
- 全 0 的情况：输出 `0`（不是一堆 0）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<string> s(n);
    for (int i = 0; i < n; i++) cin >> s[i];
    sort(s.begin(), s.end(), [](const string& a, const string& b) {
        return a+b > b+a;
    });
    string ans;
    for (auto& x : s) ans += x;
    // 去前导 0
    int p = 0;
    while (p < (int)ans.size()-1 && ans[p] == '0') p++;
    cout << ans.substr(p) << "\n";
}
```

### 8.5 Lab14-题目1：选择排序（q183, 中等, 100 分）

**题意**：选择排序（按找最小值实现），输出 n/2 长度中间结果 + 最终结果。

**关键点**：
- 外层 i = 0..n-1：找 [i..n-1] 最小，与 a[i] 交换
- 已排序区 = [0..i-1]
- 长度 = n/2 时输出

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    bool printed = false;
    for (int i = 0; i < n-1; i++) {
        int mi = i;
        for (int j = i+1; j < n; j++) if (a[j] < a[mi]) mi = j;
        swap(a[i], a[mi]);
        if (!printed && i+1 == n/2) {
            for (int k = 0; k < n; k++) cout << (k?" ":"") << a[k];
            cout << "\n"; printed = true;
        }
    }
    for (int k = 0; k < n; k++) cout << (k?" ":"") << a[k];
    cout << "\n";
}
```

---

### 8.6 Lab15-题目1：归并排序的链式实现（q189, 中等, 20 分）

**题意**：链表归并排序；输出**归并前的两个子序列**和**最终结果**。

**关键点**：
- 自顶向下：递归地把链表对半拆
- 拆到剩 1 个节点时返回
- 合并：双指针归并
- 输出：合并**之前**打印左右子序列

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
struct LNode { int v; LNode* next; LNode(int x): v(x), next(nullptr) {} };

void print(LNode* h) {
    bool f = true;
    while (h) { if (!f) cout << ' '; f=false; cout << h->v; h = h->next; }
    cout << "\n";
}

// 找中点（slow 指向左半最后一个）
LNode* midAndSplit(LNode* head) {
    LNode *slow = head, *fast = head->next;
    while (fast && fast->next) { slow = slow->next; fast = fast->next->next; }
    LNode* mid = slow->next;
    slow->next = nullptr;
    return mid;
}

LNode* merge(LNode* a, LNode* b) {
    LNode dummy(0), *tail = &dummy;
    while (a && b) {
        if (a->v <= b->v) { tail->next = a; a = a->next; }
        else              { tail->next = b; b = b->next; }
        tail = tail->next;
    }
    tail->next = a ? a : b;
    return dummy.next;
}

LNode* mergeSort(LNode* head) {
    if (!head || !head->next) return head;
    LNode* mid = midAndSplit(head);
    LNode* left  = mergeSort(head);
    LNode* right = mergeSort(mid);
    // 题目要求：归并前打印子序列
    print(left);
    print(right);
    return merge(left, right);
}

int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    LNode* head = nullptr; LNode* tail = nullptr;
    for (int i = 0; i < n; i++) {
        LNode* p = new LNode(a[i]);
        if (!head) head = tail = p;
        else { tail->next = p; tail = p; }
    }
    LNode* res = mergeSort(head);
    print(res);
    // 释放
    while (res) { LNode* t = res; res = res->next; delete t; }
}
```

### 8.7 Lab15-题目2：快速排序的顺序实现（q190, 中等, 20 分）

**题意**：顺序 List 快排，**末元素为轴点**；输出第一轮划分结果 + 最终结果。

**关键点**：
- 课本的"末元素为轴"方法（Hoare 变种）：
  - 设轴 = a[hi]
  - i = lo, j = hi
  - 从左找到第一个 > 轴的；从右找到第一个 < 轴的；交换
  - 直到 i ≥ j；最后把 a[i] 和 a[hi] 交换
- **一轮结束**：轴点左边的都 ≤ 轴，右边的都 ≥ 轴
- 递归 quicksort(lo, pivot-1)、quicksort(pivot+1, hi)
- 第一轮结束立刻打印数组

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int partition(vector<int>& a, int lo, int hi) {
    int pivot = a[hi];
    int i = lo, j = hi;
    while (i < j) {
        while (i < j && a[i] < pivot) i++;
        while (i < j && a[j] >= pivot) j--;
        if (i < j) swap(a[i], a[j]);
    }
    swap(a[i], a[hi]);
    return i;
}

void quicksort(vector<int>& a, int lo, int hi) {
    if (lo >= hi) return;
    int p = partition(a, lo, hi);
    quicksort(a, lo, p-1);
    quicksort(a, p+1, hi);
}

void print(const vector<int>& a) {
    for (int i = 0; i < (int)a.size(); i++) cout << (i?" ":"") << a[i];
    cout << "\n";
}

int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    // 手动做第一轮
    int p = partition(a, 0, n-1);
    print(a);
    quicksort(a, 0, p-1);
    quicksort(a, p+1, n-1);
    print(a);
}
```

### 8.8 Lab15-题目3：颜色排序 Sort Colors（q186, 中等, 20 分）

**题意**：0/1/2 排序，要求 O(n) 时间和 O(1) 空间。

**关键点**：**三向切分 / 荷兰国旗问题**。
- 三指针：left（下一个 0 的位置）、i（当前）、right（下一个 2 的位置）
- `a[i] == 0`：交换 a[i], a[left]，left++, i++
- `a[i] == 1`：i++
- `a[i] == 2`：交换 a[i], a[right]，right--，i 不动（因为换过来的还要检查）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<int> a; int x;
    while (cin >> x && x != -1) a.push_back(x);
    int n = a.size();
    int l = 0, r = n - 1, i = 0;
    while (i <= r) {
        if (a[i] == 0) swap(a[i++], a[l++]);
        else if (a[i] == 1) i++;
        else swap(a[i], a[r--]);
    }
    for (int k = 0; k < n; k++) cout << (k?" ":"") << a[k];
    cout << "\n";
}
```

### 8.9 Lab15-题目4：优先级队列（q192, 中等, 20 分）

**题意**：用堆（顺序存储）实现优先级队列（数字大优先）。要求：
- 初始序列 buildHeap 建堆
- 删除 m 个最大
- 插入 k 个新元素（**自下而上 siftUp**）
- 输出：初始堆 → 删除后 → 插入后

**关键点**：
- 1-indexed 数组，`parent = i/2`, `left = 2i`, `right = 2i+1`
- `siftDown`：取子节点中较大者，必要时上浮
- `siftUp`：与父节点比，必要时下沉（这里插入用 siftUp）
- 题目要求"删除 m 个" → 反复 popMax

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

class MaxHeap {
    vector<int> h;       // 1-indexed
    int n;
public:
    MaxHeap(): n(0) { h.push_back(0); }
    void siftDown(int i) {
        while (2*i <= n) {
            int j = 2*i;
            if (j+1 <= n && h[j+1] > h[j]) j++;
            if (h[i] >= h[j]) break;
            swap(h[i], h[j]);
            i = j;
        }
    }
    void siftUp(int i) {
        while (i > 1 && h[i] > h[i/2]) {
            swap(h[i], h[i/2]);
            i /= 2;
        }
    }
    void buildHeap(vector<int> a) {    // 数组 a 元素
        h.clear(); h.push_back(0);
        for (int x : a) h.push_back(x);
        n = a.size();
        for (int i = n/2; i >= 1; i--) siftDown(i);
    }
    int popMax() {
        int v = h[1];
        h[1] = h[n--];
        siftDown(1);
        return v;
    }
    void insert(int x) { h.push_back(x); n++; siftUp(n); }
    void print() const {
        for (int i = 1; i <= n; i++) cout << (i==1?"":" ") << h[i];
        cout << "\n";
    }
};

int main() {
    MaxHeap pq;
    vector<int> init;
    int x;
    while (cin >> x && x != -1) init.push_back(x);
    pq.buildHeap(init);
    pq.print();
    int m; cin >> m;
    for (int i = 0; i < m; i++) pq.popMax();
    pq.print();
    while (cin >> x && x != -1) pq.insert(x);
    pq.print();
}
```

### 8.10 Lab15-题目5：堆排序的顺序实现（q191, 中等, 20 分）

**题意**：对顺序 List 做堆排序；输出**初始大根堆** + **最终升序**。

**关键点**：
- 1-indexed：buildHeap（O(n)）+ 反复交换 h[1] 与 h[i]，n--，siftDown(1)
- **输出顺序**：初始堆是 buildHeap 后的数组；最终是排好序的（升序）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

void siftDown(vector<int>& h, int n, int i) {
    while (2*i <= n) {
        int j = 2*i;
        if (j+1 <= n && h[j+1] > h[j]) j++;
        if (h[i] >= h[j]) break;
        swap(h[i], h[j]);
        i = j;
    }
}

void heapSort(vector<int>& h) {   // 1-indexed
    int n = (int)h.size() - 1;
    for (int i = n/2; i >= 1; i--) siftDown(h, n, i);
    // 第一阶段完成：初始大根堆
    for (int k = 1; k <= n; k++) cout << (k==1?"":" ") << h[k];
    cout << "\n";
    // 第二阶段
    for (int i = n; i > 1; i--) {
        swap(h[1], h[i]);
        siftDown(h, i-1, 1);
    }
    for (int k = 1; k <= n; k++) cout << (k==1?"":" ") << h[k];
    cout << "\n";
}

int main() {
    int n; cin >> n;
    vector<int> h(1);
    for (int i = 0; i < n; i++) { int x; cin >> x; h.push_back(x); }
    heapSort(h);
}
```

---


## 第 9 章 哈希表

### 章节速查

**核心主题**：哈希函数 + 冲突处理

| 概念 | 关键点 |
|---|---|
| 哈希函数 | key → [0, m) 的映射，要求简单/均匀/一致 |
| 冲突 | 不同 key → 同一地址 |
| 同义词 | 哈希值相同的不同 key |
| 装填因子 | α = n/m，越小越好 |

**冲突解决方法对比**：

| 方法 | 思想 | 优缺点 |
|---|---|---|
| 链地址法 | 桶 → 链表 | ✅ 简单 / ❌ 指针开销 |
| 线性探测 | `H_i = (H+i) mod m` | ✅ 简单 / ❌ 堆积 |
| 平方探测 | `H_i = (H+i²) mod m` | ✅ 缓解堆积 / ❌ 不能探遍 |
| 双散列 | `H_i = (H+i·H₂) mod m` | ✅ 均匀 / ❌ 需两个哈希 |

**哈希表删除**：
- 链地址法：直接删节点 ✅
- 开放定址法：**墓碑标记**（EMPTY / OCCUPIED / DELETED），不能直接置空

**复杂度**：期望 O(1)，最坏 O(n)

**Lab 注意事项**：
- BKDRHash：`h = h*seed + ch`，seed 常取 31/131
- HashMap（链地址法）：每个桶维护一个 vector
- Longest Substring Without Repeating：**滑动窗口 + 哈希表**记录字符最后出现位置

### 本章 Lab 代码（完整）

### 9.2 Lab17-题目1：Hash Table 的顺序实现（q202, 中等, 100 分）

**题意**：平方探测 + 取余法（key % n）。要求：
- 重复 key 只存第一个
- 探测次数上限 = 课本定义（一般是 `n/2` 或"上溢"）
- 输出：探测到 key1 的过程 + key1 的 value；探测到 key2 的过程；找不到输出 `NULL`

**关键点**：
- 表大小 n 选质数
- 平方探测：`for (i=0; i<n; i++) idx = (H + i*i) % n`（只 +i²），或 ±i²
- 课本惯例：探测 `H, H+1, H+4, H+9, ...`
- 探测失败：`idx` 第一次为空时（未占用）就停
- 探测到 key1 → 输出所有走过的 idx + "key1 value"
- 找不到 → 输出走过的 idx + `NULL`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m; cin >> n >> m;
    vector<int> key(n, -1), val(n, -1);    // -1 表示空

    for (int i = 0; i < m; i++) {
        int k, v; cin >> k >> v;
        int h = k % n;
        int j = 0, idx;
        for (; j < n; j++) {
            idx = (h + j*j) % n;
            if (key[idx] == -1 || key[idx] == k) break;
        }
        if (key[idx] == -1) { key[idx] = k; val[idx] = v; }
        // 重复不覆盖
    }
    // 查询
    auto query = [&](int k) {
        int h = k % n;
        vector<int> path;
        for (int j = 0; j < n; j++) {
            int idx = (h + j*j) % n;
            path.push_back(idx);
            if (key[idx] == -1) return make_pair(path, -1);   // NULL
            if (key[idx] == k) return make_pair(path, val[idx]);
        }
        return make_pair(path, -1);
    };
    int k1, k2; cin >> k1 >> k2;
    for (int q : {k1, k2}) {
        auto [path, v] = query(q);
        for (size_t i = 0; i < path.size(); i++) cout << (i?" ":"") << path[i];
        if (v == -1) cout << " NULL";
        else         cout << " " << q << " " << v;
        cout << "\n";
    }
}
```

### 9.3 Lab16-题目1：HashMap 的实现（q203, 中等, 50 分）

**题意**：用**链地址法**实现 HashMap，支持 `put` / `get` / `remove`。字符串 key 用 **BKDRHash**。要求：
- put 重复 key → 更新 value
- get 不存在 → 返回 -1
- 输出：插入某 key 的地址 + 删除某 key 的过程（找不到输出 NULL）+ 查询某 key 的过程（NULL 输出 NULL）

**关键点**：
- 数组 `vector<list<pair<string,string>>>` 存桶
- BKDRHash 把字符串映射到 [0, m)
- 输出格式：先输出 key 的地址码，然后是过程（"key1 key2"）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

unsigned int BKDR(const string& s, unsigned int size) {
    unsigned int h = 0, seed = 31;
    for (char c : s) h = (h * seed + (unsigned char)c) % size;
    return h;
}

int main() {
    int n, m; cin >> n >> m;
    vector<list<pair<string,string>>> bucket(n);
    for (int i = 0; i < m; i++) {
        string k, v; cin >> k >> v;
        unsigned int idx = BKDR(k, n);
        bool found = false;
        for (auto& p : bucket[idx]) if (p.first == k) { p.second = v; found = true; break; }
        if (!found) bucket[idx].push_front({k, v});
    }
    // 第 3 行：插入（输出地址码）
    string k3, v3; cin >> k3 >> v3;
    unsigned int idx3 = BKDR(k3, n);
    bool found = false;
    for (auto& p : bucket[idx3]) if (p.first == k3) { p.second = v3; found = true; break; }
    if (!found) bucket[idx3].push_front({k3, v3});
    cout << idx3 << "\n";

    // 第 4 行：删除（输出地址 + "key" + "NULL" 如果失败）
    string k4; cin >> k4;
    unsigned int idx4 = BKDR(k4, n);
    bool removed = false;
    for (auto it = bucket[idx4].begin(); it != bucket[idx4].end(); ++it) {
        if (it->first == k4) { bucket[idx4].erase(it); removed = true; break; }
    }
    cout << idx4 << ' ' << k4 << ' ' << (removed ? "" : "NULL") << "\n";
    // 注意：题目样例 "6 dog NULL" 实际是：地址 dog 删除结果(NULL)
    // 严格按样例输出格式

    // 第 5 行：查询
    string k5; cin >> k5;
    unsigned int idx5 = BKDR(k5, n);
    cout << idx5 << ' ';
    for (auto& p : bucket[idx5]) cout << p.first << (p.first==k5?"":" ");  // 简化
    cout << " " << k5 << " " << (查询到的value) << "\n";
}
```

> 💡 **注**：Lab16 输出格式细节较多（要打印桶内遍历到的 key），考试时按题目样例**严格照搬格式**。上面模板给出主要框架，遇到样例格式不同按样例调整。

### 9.4 Lab16-题目2：Longest Substring Without Repeating Characters（q205, 中等, 50 分）

**题意**：字符串中无重复字符的最长子串长度。

**关键点**：**滑动窗口 + 哈希表记录上次出现位置**。
- `last[ch]` = ch 上次出现的位置（-1 表示没出现）
- 遍历 i = 0..n-1
- 维护左边界 `lo`：`lo = max(lo, last[s[i]] + 1)`（如果 ch 出现过）
- 更新答案：`ans = max(ans, i - lo + 1)`
- 更新 `last[s[i]] = i`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    string s; cin >> s;
    vector<int> last(256, -1);
    int lo = 0, ans = 0;
    for (int i = 0; i < (int)s.size(); i++) {
        lo = max(lo, last[s[i]] + 1);
        ans = max(ans, i - lo + 1);
        last[s[i]] = i;
    }
    cout << ans << "\n";
}
```

---


## 第 10 章 二叉树

### 章节速查

**核心主题**：二叉树 + BST + AVL + 红黑树

| 概念 | 关键点 |
|---|---|
| 满二叉树 | 所有叶子都在最后一层，节点数 2^h-1 |
| 完全二叉树 | 叶子只缺右边，数组存储最自然 |
| 二叉查找树 | 左 < 根 < 右 |
| AVL | 平衡二叉树，4 种旋转恢复平衡 |
| 红黑树 | 自平衡，5 大性质保证 O(logn) |

**遍历（必背）**：

| 顺序 | 递归顺序 | 栈序 |
|---|---|---|
| 前序 | 根左右 | 根入栈即访问 |
| 中序 | 左根右 | 出栈时访问 |
| 后序 | 左右根 | 第二次访问才输出 |
| 层序 | BFS | 队列 |

**复杂度**：
- 二叉树查找（平衡）：O(logn)
- BST 删除 3 种情况：叶子 / 单子树 / 双子树（找前驱或后继替换）

**Lab 注意事项**：
- 二叉树遍历：完全二叉树用数组存（`i → 2i+1, 2i+2`）
- BST 插入/查找/删除：递归实现
- LCA（最近公共祖先）：同时从根遍历，分叉点即 LCA

### 本章 Lab 代码（完整）

### 10.2 Lab19-题目1：二叉树遍历（q27, 中等, 100 分）

**题意**：把输入序列构造成**完全二叉树**，输出：
- 第 1 行：树高度（仅 1 个节点 = 0）
- 第 2 行：前序
- 第 3 行：中序
- 第 4 行：后序

**关键点**：
- 完全二叉树的数组下标规则：i 节点的左子 = 2i+1，右子 = 2i+2（0-indexed）
- 树高 = `⌊log₂ n⌋`（当 n = 2^k - 1 时等于 k-1；通用 `floor(log2(n))`）
- 递归遍历

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;
int n;
vector<int> a;

// 完全二叉树：i 的左子 2i+1，右子 2i+2（0-indexed）
int height(int i) {
    if (i >= n) return -1;
    return 1 + max(height(2*i+1), height(2*i+2));
}
void pre(int i)  { if (i>=n) return; cout << a[i] << " "; pre(2*i+1); pre(2*i+2); }
void in(int i)   { if (i>=n) return; in(2*i+1); cout << a[i] << " "; in(2*i+2); }
void post(int i) { if (i>=n) return; post(2*i+1); post(2*i+2); cout << a[i] << " "; }

int main() {
    cin >> n;
    a.resize(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    cout << height(0) << "\n";
    pre(0);  cout << "\n";
    in(0);   cout << "\n";
    post(0); cout << "\n";
}
```

### 10.3 Lab18-题目1：二叉查找树（q209, 中等, 33 分）

**题意**：BST 实现插入、删除（**用前驱**）、查找；输入单词（key + 中文），输出查询路径 + 内容；不存在输出 `NULL`。

**关键点**：
- BST 节点：`{string key, string other; Node *l, *r;}`
- 插入：标准 BST 插入
- 删除：双子树用**前驱**（左子树最大）
- 查找：从根开始记录路径，输出路径上每个节点的 key
- 找不到时路径继续到 NULL，输出 `NULL`

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// BST 节点
class TNode {
public:
    string key, other;
    TNode *l, *r;
    TNode(string k, string o): key(k), other(o), l(nullptr), r(nullptr) {}
};

// BST 封装
class BST {
    TNode* root;
    TNode* insertRec(TNode* p, string k, string o) {
        if (!p) return new TNode(k, o);
        if (k < p->key)      p->l = insertRec(p->l, k, o);
        else if (k > p->key) p->r = insertRec(p->r, k, o);
        return p;
    }
    TNode* findMax(TNode* p) { while (p->r) p = p->r; return p; }
    TNode* removeRec(TNode* p, string k) {
        if (!p) return nullptr;
        if (k < p->key)      p->l = removeRec(p->l, k);
        else if (k > p->key) p->r = removeRec(p->r, k);
        else {
            if (!p->l) { TNode* t = p->r; delete p; return t; }
            if (!p->r) { TNode* t = p->l; delete p; return t; }
            TNode* pre = findMax(p->l);          // 用前驱
            p->key = pre->key; p->other = pre->other;
            p->l = removeRec(p->l, pre->key);
        }
        return p;
    }
    void clear(TNode* p) { if (!p) return; clear(p->l); clear(p->r); delete p; }
public:
    BST(): root(nullptr) {}
    ~BST() { clear(root); }
    void insert(string k, string o) { root = insertRec(root, k, o); }
    void remove(string k)            { root = removeRec(root, k); }
    void query(string k) const {
        TNode* p = root;
        bool first = true;
        while (p) {
            if (!first) cout << " ";
            first = false;
            cout << p->key;
            if (k == p->key) { cout << " " << p->key << " " << p->other << "\n"; return; }
            p = (k < p->key) ? p->l : p->r;
        }
        cout << " NULL\n";
    }
};

int main() {
    int n; cin >> n;
    BST tree;
    for (int i = 0; i < n; i++) { string k, o; cin >> k >> o; tree.insert(k, o); }
    int d; cin >> d;
    for (int i = 0; i < d; i++) { string k; cin >> k; tree.remove(k); }
    string t1, t2; cin >> t1 >> t2;
    tree.query(t1);
    tree.query(t2);
}
```

### 10.4 Lab18-题目2：The Sum of Path（q215, 困难, 33 分）

**题意**：给定插入序列生成**BST**，判断是否存在根→叶路径节点和 = k。同时输出树的高度（仅 1 节点 = 0）。

**关键点**：
- 按 BST 插入方式建树（数值序列就是插入顺序）
- 高度 = `floor(log2(n))` 对**完全**BST？不一定，**一般树高**用递归算
- 路径和 = k：DFS，传入当前和，递归到叶节点判断
- 数值**不要求不重复**（样例里有重复），BST 重复节点按"≥"放右子树（约定）

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// BST 节点
class TNode {
public:
    int v;
    TNode *l, *r;
    TNode(int x): v(x), l(nullptr), r(nullptr) {}
};

// BST 封装
class BST {
    TNode* root;
    TNode* insertRec(TNode* p, int x) {
        if (!p) return new TNode(x);
        if (x < p->v) p->l = insertRec(p->l, x);
        else          p->r = insertRec(p->r, x);
        return p;
    }
    int heightRec(TNode* p) const { return p ? 1 + max(heightRec(p->l), heightRec(p->r)) : -1; }
    bool hasPathRec(TNode* p, int sum) const {
        if (!p) return false;
        if (!p->l && !p->r) return p->v == sum;
        return hasPathRec(p->l, sum - p->v) || hasPathRec(p->r, sum - p->v);
    }
public:
    BST(): root(nullptr) {}
    void insert(int x) { root = insertRec(root, x); }
    int  height() const { return heightRec(root); }
    bool hasPathSum(int k) const { return hasPathRec(root, k); }
};

int main() {
    int n; cin >> n;
    BST tree;
    for (int i = 0; i < n; i++) { int x; cin >> x; tree.insert(x); }
    int k; cin >> k;
    cout << tree.height() << ' ' << (tree.hasPathSum(k) ? "true" : "false") << "\n";
}
```

### 10.5 Lab18-题目3：二分查找树的最矮公共祖先（q216, 中等, 34 分）

**题意**：BST 找 p 和 q 的 LCA（最低公共祖先）。

**关键点**：**BST 的 LCA 利用有序性**：
- 从根开始
- 若 p、q 都 < 根 → 递归左
- 若 p、q 都 > 根 → 递归右
- 否则（p ≤ 根 ≤ q 或反过来）→ 根就是 LCA

**C++ 模板**：
```cpp
#include <bits/stdc++.h>
using namespace std;

// BST 节点
class TNode {
public:
    int v;
    TNode *l, *r;
    TNode(int x): v(x), l(nullptr), r(nullptr) {}
};

// BST 封装
class BST {
    TNode* root;
    TNode* insertRec(TNode* p, int x) {
        if (!p) return new TNode(x);
        if (x < p->v) p->l = insertRec(p->l, x);
        else          p->r = insertRec(p->r, x);
        return p;
    }
    // 利用 BST 有序性求 LCA
    int lcaRec(TNode* p, int a, int b) const {
        while (p) {
            if (b < p->v) p = p->l;
            else if (a > p->v) p = p->r;
            else return p->v;
        }
        return -1;
    }
public:
    BST(): root(nullptr) {}
    void insert(int x) { root = insertRec(root, x); }
    int lca(int p, int q) const {
        int a = min(p, q), b = max(p, q);
        return lcaRec(root, a, b);
    }
};

int main() {
    int n; cin >> n;
    BST tree;
    for (int i = 0; i < n; i++) { int x; cin >> x; tree.insert(x); }
    int p, q; cin >> p >> q;
    cout << tree.lca(p, q) << "\n";
}
```

---


## 第 11 章 多叉树

### 章节速查

**核心主题**：Trie + B 树 + B+ 树

| 概念 | 关键点 |
|---|---|
| Trie | 字典树，每个节点是一个字符 |
| B 树 | 多路平衡查找树，所有叶子同层 |
| B+ 树 | 数据只在叶子，叶子链表相连 |

**复杂度**：

| 操作 | Trie | B 树 | B+ 树 |
|---|---|---|---|
| 查找 | O(字符串长度) | O(logn) | O(logn) |
| 插入 | O(字符串长度) | O(logn) | O(logn) |
| 空间 | O(字符数 × 字母表) | O(n) | O(n) |

**核心应用**：
- Trie：自动补全、拼写检查、IP 路由
- B 树 / B+ 树：数据库索引（**减少磁盘 IO**）

> 第 11 章为理论章节（多叉树 / 图），相关 Lab 题目分散在前 10 章的二叉树 / 排序 / 查找中。


## 第 12 章 图

### 章节速查

**核心主题**：图的存储 + 遍历 + 经典算法

| 存储 | 空间 | 适用 |
|---|---|---|
| 邻接矩阵 | O(V²) | 稠密图、快速判边 |
| 邻接表 | O(V+E) | 稀疏图（最常用）|
| 边表 | O(E) | Kruskal |
| 前向星 | O(E) | 链式前向星 |

**经典算法对比**：

| 算法 | 用途 | 复杂度 | 适用 |
|---|---|---|---|
| DFS | 遍历/拓扑/连通分量 | O(V+E) | 任意图 |
| BFS | 最短路（无权） | O(V+E) | 任意图 |
| 拓扑排序 | AOV 网 | O(V+E) | DAG |
| Dijkstra | 单源最短路 | O((V+E)logV) | 非负权 |
| Floyd | 全对最短路 | O(V³) | 任意权、小规模 |
| Prim | MST | O((V+E)logV) | 稠密图 |
| Kruskal | MST | O(ElogE) | 稀疏图 |
| Kosaraju | SCC | O(V+E) | 任意有向图 |

**Lab 注意事项**：
- 迷宫 BFS：queue + dist[][]
- 拓扑排序：Kahn 算法（入度为 0 的进队列）/ DFS 法
- Dijkstra 堆优化：`priority_queue<dist, node>` 取当前最短
- 并查集：find + union + path compression

> 第 12 章为理论章节（多叉树 / 图），相关 Lab 题目分散在前 10 章的二叉树 / 排序 / 查找中。


## 附录 A: 51 道 Lab 必写清单（按 questionId）

> 冲刺顺序建议：从上到下；每题先看 §X.Y 模板，再自己复写一遍，**不参考**。卡住再看。

| # | qid | Lab | 章节 | 标题 | 难度 | 满分 | 关键点 |
|---:|---:|---|---|---|---|---:|---|
| 1 | 136 | Lab01 | 1.2 | One-Dimensional Life | 中 | 50 | 4 邻居规则 + 60 行宽 |
| 2 | 135 | Lab01 | 1.3 | Magic Square | 中 | 50 | 右上对角 + 撞占位向下 |
| 3 | 137 | Lab23 | 2.2 | 栈的顺序实现 | 难 | 100 | 逆序输出 + 完整 ADT |
| 4 | 158 | Lab05 | 2.3 | 链式存储的栈 | 难 | 100 | 头插法 + 析构释放 |
| 5 | 139 | Lab02 | 2.4 | 后序波兰式 10 以内 | 中 | 33 | 一行 getline + 非法判空 |
| 6 | 141 | Lab02 | 2.5 | 出入栈序列判断 | 中 | 33 | 模拟栈 sz 计数 |
| 7 | 142 | Lab02 | 2.6 | 括号匹配（注释跳过） | 中 | 34 | inBlock/inLine 双标志 |
| 8 | 143 | Lab20 | 2.7 | 中序波兰式求解 | 简单 | 100 | 双栈 + 优先级 + 浮点 3 位 |
| 9 | 146 | Lab24 | 3.2 | 环形队列 | 中 | 100 | 模运算 + count 变量 |
| 10 | 148 | Lab03 | 3.3 | 字符串平衡 | 中 | 33 | find(':') + 五种情况 |
| 11 | 147 | Lab03 | 3.4 | 双端队列 | 中 | 33 | deque 存最近 n 条 |
| 12 | 150 | Lab03 | 3.5 | 团队队列 | 中 | 34 | 嵌套 deque + 团队顺序 |
| 13 | 159 | Lab06 | 4.2 | 链式存储的队列 | 难 | 50 | 头尾指针 + 析构 |
| 14 | 155 | Lab04 | 4.3 | 约瑟夫问题 | 中 | 20 | 循环链表 + cur 前移 m-1 |
| 15 | 160 | Lab06 | 4.4 | 多项式运算 | 中 | 50 | 链式 + 双指针归并 + 输出格式 |
| 16 | 103 | Lab25 | 5.2 | 递归 Min/Sum | 难 | 50 | 终止 n==1 |
| 17 | 161 | Lab25 | 5.3 | 二进制转十进制 | 中 | 50 | 字符串尾递归 |
| 18 | 163 | Lab07 | 5.4 | 全排列 | 中 | 50 | 回溯 + used 数组 |
| 19 | 164 | Lab07 | 5.5 | 迷宫 BFS | 中 | 50 | BFS 求最短路 + 起终点 +1 |
| 20 | 151 | Lab22 | 6.2 | 单链表创建与输出 | 难 | 100 | 头尾指针 + 求和 |
| 21 | 153 | Lab04 | 6.3 | 链表指定值插入（位置） | 难 | 20 | 输出位置 = i+1 |
| 22 | 154 | Lab04 | 6.4 | 链表指定值删除 | 难 | 20 | 数组模拟最简 |
| 23 | 156 | Lab04 | 6.5 | 链表置逆 | 中 | 20 | reverse 即可 |
| 24 | 157 | Lab04 | 6.6 | 链表去重 | 中 | 20 | set seen |
| 25 | 165 | Lab08 | 6.7 | List 顺序实现 | 难 | 50 | 完整 ADT + 顺序输出 |
| 26 | 168 | Lab08 | 6.8 | List 单链表实现 | 难 | 50 | 哑节点 + 最后访问位置 |
| 27 | 169 | Lab09 | 6.9 | List 双链表实现 | 难 | 33 | 双向哑节点 + 正逆序输出 |
| 28 | 167 | Lab09 | 6.10 | 页面相关系数 | 中 | 33 | MyList + 找 max 输出所有 |
| 29 | 166 | Lab09 | 6.11 | 大数运算 | 简单 | 34 | vector<int> 低位在前 |
| 30 | 170 | Lab10 | 6.12 | String 实现 | 中 | 100 | List<char> 内部 + 字典序 |
| 31 | 172 | Lab11 | 7.2 | 前后向最近邻 | 中 | 33 | O(n²) 双扫 |
| 32 | 176 | Lab11 | 7.3 | Sqrt 函数 | 中 | 33 | 二分 + 防溢出 |
| 33 | 175 | Lab11 | 7.4 | StartEnd Position | 中 | 34 | lowerBound 两次 |
| 34 | 178 | Lab12 | 7.5 | 二分查找单词 | 中 | 100 | 二分 + Record |
| 35 | 180 | Lab13 | 8.2 | 插入排序（顺序） | 中 | 33 | n/2 时输出 |
| 36 | 188 | Lab13 | 8.3 | 插入排序（链式） | 中 | 33 | 已排序链 + 头插/中间插 |
| 37 | 184 | Lab13 | 8.4 | Largest Number | 中 | 34 | sort + a+b > b+a |
| 38 | 183 | Lab14 | 8.5 | 选择排序 | 中 | 100 | n/2 时输出 |
| 39 | 189 | Lab15 | 8.6 | 归并排序（链式） | 中 | 20 | 中点拆分 + 合并前打印 |
| 40 | 190 | Lab15 | 8.7 | 快速排序（顺序） | 中 | 20 | 课本末元素为轴 |
| 41 | 186 | Lab15 | 8.8 | 颜色排序 | 中 | 20 | 三向切分 |
| 42 | 192 | Lab15 | 8.9 | 优先级队列 | 中 | 20 | buildHeap + siftUp/Down |
| 43 | 191 | Lab15 | 8.10 | 堆排序 | 中 | 20 | 1-indexed 堆 |
| 44 | 202 | Lab17 | 9.2 | 顺序哈希表（平方探测） | 中 | 100 | 平方探测 + 路径输出 |
| 45 | 203 | Lab16 | 9.3 | 链地址 HashMap | 中 | 50 | BKDRHash + list 桶 |
| 46 | 205 | Lab16 | 9.4 | 最长不重复子串 | 中 | 50 | 滑动窗口 |
| 47 | 27 | Lab19 | 10.2 | 二叉树遍历（完全） | 中 | 100 | 数组 + 递归遍历 |
| 48 | 209 | Lab18 | 10.3 | BST 插入/删除/查找 | 中 | 33 | 路径 + 前驱删除 |
| 49 | 215 | Lab18 | 10.4 | Sum of Path | 难 | 33 | BST + DFS 根到叶 |
| 50 | 216 | Lab18 | 10.5 | BST LCA | 中 | 34 | 利用 BST 有序性 |
| 51 | 134 | Lab21 | 1.4 | 二维 Life Game | 中 | 100 | 8 邻居 + 20×60 网格 |

---


## 附录 B: 复杂度速查表

### B.1 数据结构

| 结构 | 访问 | 查找 | 插入 | 删除 |
|---|---|---|---|---|
| 数组 | O(1) | O(n) | O(n) | O(n) |
| 单链表 | O(n) | O(n) | O(1)* | O(1)* |
| 双向链表 | O(n) | O(n) | O(1)* | O(1)* |
| 栈 | O(n) | O(n) | O(1) | O(1) |
| 队列 | O(n) | O(n) | O(1) | O(1) |
| 哈希表 | - | O(1) 均 | O(1) 均 | O(1) 均 |
| BST | O(log n) 均 | O(log n) 均 | O(log n) 均 | O(log n) 均 |
| AVL | O(log n) | O(log n) | O(log n) | O(log n) |
| 红黑树 | O(log n) | O(log n) | O(log n) | O(log n) |
| B 树 | O(log n) | O(log n) | O(log n) | O(log n) |

*已知位置时

### B.2 排序

| 排序 | 平均 | 最坏 | 空间 | 稳定 |
|---|---|---|---|---|
| 冒泡 | O(n²) | O(n²) | O(1) | ✅ |
| 插入 | O(n²) | O(n²) | O(1) | ✅ |
| 希尔 | O(n^1.3) | O(n²) | O(1) | ❌ |
| 选择 | O(n²) | O(n²) | O(1) | ❌ |
| 堆排 | O(n log n) | O(n log n) | O(1) | ❌ |
| 归并 | O(n log n) | O(n log n) | O(n) | ✅ |
| 快排 | O(n log n) | O(n²) | O(log n) | ❌ |
| 计数 | O(n+k) | O(n+k) | O(k) | ✅ |
| 基数 | O(d·n) | O(d·n) | O(n+k) | ✅ |
| 桶 | O(n+k) | O(n²) | O(n+k) | ✅ |

### B.3 查找

| 查找 | 时间 | 前提 |
|---|---|---|
| 顺序 | O(n) | 无 |
| 二分 | O(log n) | 有序 |
| 插值 | O(log log n) | 均匀分布 |
| 斐波那契 | O(log n) | 有序 |
| 分块 | O(√n) | 分块有序 |
| 哈希 | O(1) 均 | 设计良好 |
| BST | O(log n) 均 | 平衡 |
| B 树 | O(log n) | 外存 |

### B.4 图算法

| 算法 | 时间 | 适用 |
|---|---|---|
| BFS | O(V+E) | 无权最短路 |
| DFS | O(V+E) | 拓扑、连通性 |
| Dijkstra | O((V+E) log V) | 单源、非负权 |
| Bellman-Ford | O(VE) | 单源、负权 |
| SPFA | 平均 O(E) | 单源、负权 |
| Floyd | O(V³) | 全对最短路 |
| Prim | O(E log V) | MST |
| Kruskal | O(E log E) | MST |
| 拓扑排序 | O(V+E) | DAG |
| Kosaraju | O(V+E) | SCC |
| Tarjan | O(V+E) | SCC |

### B.5 常用数量级

| n | 1s 内可承受 |
|---|---|
| n ≤ 10⁴ | O(n²) |
| n ≤ 10⁵ | O(n log n) |
| n ≤ 10⁶ | O(n) |
| n ≤ 10⁷ | O(n) + 高效 I/O |
| n ≤ 10⁸ | 极少 O(n)，需要 O(1) 步/元素 |

---


## 附录 C: 常见坑位速查

| 坑 | 避坑 |
|---|---|
| `mid*mid` 溢出 int | 用 `long long` |
| 环形队列 front==rear 判空还是判满 | 用 count 变量更稳 |
| 链表反转没保存 next | 一定要先存 `nxt = cur->next` |
| BST 删除双子树 | 用前驱或后继，再删前驱/后继 |
| 中缀表达式负数 | 表达式开头 / `(` 后 / 运算符后：补 0 |
| 输出空格 | 行内空格分隔，行末不空格 |
| 输入 `-1` 结束 | 一定要在读入循环里 break |
| 排序中间结果 | 在**插入/选择次数 = n/2** 时输出 |
| 堆 1-indexed vs 0-indexed | 父子关系公式差 1 |
| 哈希表删除 | 链地址法可直接删；开放定址要墓碑 |
| 哈希表取模 | 质数 p |
| 顺序表扩容 | Lab 题目固定容量即可（题目给 <100 等） |
| 字符串 hash | BKDR 公式 `h = h*seed + ch` |
| Floyd 二维数组 | `dist[i][j] = min(dist[i][j], dist[i][k]+dist[k][j])` |
| DFS 递归深度 | 树深 ≤ 10⁴，递归可能栈溢出（Lab 不涉及超大） |
| AVL 旋转 | 失衡点在祖先，回溯到第一个失衡的调整 |
| 红黑树 | 5 大性质，插入/删除旋转+变色 |
| B 树分裂 | 中间 key 上升，左右两半 |
| 拓扑排序 | Kahn 用入度，DFS 用后序 |
| MST | 切割性质、环性质 |
| 关键路径 | 最早开始 = 入点最大；最晚开始 = 出点最小 - 持续 |
| 强连通 | Kosaraju 两遍 DFS（反图） |

---


## 附录 D: 临考检查清单

- [ ] **理论题**：每章 5 分钟能复述 3 个核心概念
- [ ] **代码题**：每道 Lab 都能在 15-20 分钟内默写模板
- [ ] **复杂度**：能立即说出每种排序、查找、树的平均/最坏时间
- [ ] **输入输出**：确认每个 Lab 的边界（`-1` 结束、`-1 -1` 结束、行数）
- [ ] **特例**：空串、空树、n=1、n=0
- [ ] **手算样例**：每个模板用题目样例走一遍

---

**冲刺时长建议**：
- 第 1-2 天：理论精读（§0-§12 理论部分），重点看排序、树、图
- 第 3-5 天：按章节刷 Lab，每题先看模板 → 关掉 → 自己写 → 跑样例
- 第 6-7 天：复盘 + 默写 5 道最容易错的（多项式、堆排序、平方探测、BST 删除、迷宫 BFS）

> 🚀 **理论 + 代码**双线并进，Lab51 全掌握，第 11-12 章重点看概念题和大题（拓扑 / 最短路 / MST）。

---

**总文档大小**：约 4500 行，覆盖 12 章全理论 + 51 道 Lab 完整 C++ 模板。
